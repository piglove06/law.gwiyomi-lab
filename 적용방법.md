# 적용 방법 (v0.7 · 2026-08-07)

## 1. 파일 덮어쓰기

`files` 폴더 구조에 맞춰 그대로 덮어쓰세요.

```
files\
 ├ law_client.py      ← 교체
 ├ ai_client.py       ← 교체
 ├ main.py            ← 교체
 ├ CHANGELOG.md       ← 교체 (없으면 새로 생성)
 ├ requirements.txt   ← 교체
 ├ README.md          ← 교체
 ├ .env               ← 그대로 두세요! 덮어쓰지 마세요
 ├ .venv\             ← 그대로
 └ static\
    └ index.html      ← 교체
```

**`.env` 는 절대 덮어쓰지 마세요.** 인증키가 들어 있습니다.
(`.env.example` 은 참고용이니 덮어쓰셔도 무방합니다)

## 2. 서버 재시작

`--reload` 로 켜두셨다면 자동 재시작됩니다.
터미널에 `Reloading...` 이 뜨는지 확인하세요.

수동으로 하시려면 터미널에서 `Ctrl+C` 후:
```
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
```

## 3. 브라우저 새로고침

`Ctrl+F5` (일반 새로고침은 옛 화면이 남습니다)

## 4. 확인

드롭다운을 바꿔가며 각각 한 번씩 테스트해 보세요.

| 드롭다운 | 시험 질문 |
|---|---|
| 법령 | 주유소 토양오염도검사에서 우려기준을 넘으면 어떤 절차를 밟아야 하나 |
| 행정규칙 | 특정토양오염관리대상시설 점검은 어떻게 하나 |
| 자치법규 | 성남시 물환경 보전활동 지원 |
| 법령해석례 | 토양정밀조사 명령을 두 번 받으면 별개 처분인가 |

## 5. 아직 안 하신 것 (선택)

- `main.py` 의 `# secure=True,` 주석 풀기 → HTTPS 도메인 연결 후에
- `.env` 의 `APP_PASSWORD` 를 인증키와 다른 값으로 변경
- Cloudflare named tunnel 연결 (`law.gwiyomi-lab.com`)
