# 법령 조회 도우미 — 변경 이력

성남시청 환경과 업무 지원용 법령 조회 도구.
법제처 국가법령정보 OPEN API + Google Gemini.

---

## 버전 표기 규칙

`v메이저.마이너` — 마이너는 기능 추가/수정, 메이저는 구조 변경.

---

## v1.28 — 2026-08-19 — 전체 코드 검토

`main.py` / `ai_client.py` / `law_client.py` / `static/index.html` 을 통째로
훑었습니다. 아래는 **전부 실행으로 재현한 뒤** 고친 것들입니다. 추측으로 고친
것은 없습니다.

### 수정 — LLM 오류·한도 초과가 전부 "연결 끊김" 으로 보이던 문제 (심각)

`_ask_sync()` 는 오류 경로에서 `JSONResponse` **객체**를 돌려줍니다
(`_err` / `_quota_response`). 예전에는 그 객체를 그대로 반환했는데, 지금
`/api/ask` 는 NDJSON 스트림이라 `json.dumps` 로 감싸야 합니다.

```python
yield json.dumps({"result": result}, ensure_ascii=False) + "\n"
#   → TypeError: Object of type JSONResponse is not JSON serializable
```

이 예외가 `try` **밖**이라 스트림이 중간에 끊기고, 화면에는 원인 대신
"서버 응답이 중간에 끊겼습니다" 만 떴습니다. 결과적으로

- Ollama 가 죽었을 때 → "연결 끊김"
- 로컬 모델 타임아웃 → "연결 끊김"
- 무료 한도 초과 → "연결 끊김" (**한도 팝업은 한 번도 뜰 수 없었습니다**)

→ `JSONResponse` 면 본문을 풀어서 내보내고, `json.dumps` 자체도 감쌌습니다.

### 수정 — "아니오" 라고 답해도 그 법령이 안 빠지던 문제 (기능이 죽어 있었음)

부정 조건 필터(`_excluded_terms`)가 **검색 경로 안에만** 있었습니다.
그런데 그 경로는 `round=0`(= `answered` 가 빈 새 질문)만 지나가고, 답이
실제로 들어오는 `round>=1` 은 항상 캐시로 빠져나가 필터를 건너뛰었습니다.

즉 "방사성폐기물에 해당하나요? → **아니오**" 라고 답해도 방사성폐기물 관련
법령이 그대로 남아 답변 컨텍스트에 들어갔습니다. v1.24 에 기능 추가로
적어 뒀지만 **정상 경로에서는 한 번도 동작한 적이 없습니다.**

→ 되묻기 답을 손에 쥔 `_ask_after_search()` 로 옮겼습니다. 캐시가 오염되지
  않도록 사본에만 적용하고, `flat`·`catalog` 도 같이 줄입니다.

### 수정 — 답변의 조문 링크가 **다른 법률**로 걸리던 문제 (심각)

`XREF_RE` 의 접두사 `(법|영|규칙)` 에 단어 경계가 없어서 **앞 낱말의 마지막
글자**가 접두사로 잡혔습니다. 한국 법령명은 거의 전부 "…법" 으로 끝납니다.

```
"위험물안전관리법 제5조에 따라"
  → '법' 을 접두사로 읽고 모법(토양환경보전법) 제5조로 링크
"측정 방법 제3조",  "환경분쟁 조정법 제4조"  도 같은 경로로 오링크
```

조문 오인용을 막으려고 만든 앱에서 **링크가 오인용을 만들고** 있었습니다.
→ 앞이 한글이면 접두사로 보지 않습니다. "이 법 제13조" / "영 제8조" /
  "규칙 제12조" 는 그대로 동작합니다(재현 확인).

### 수정 — 보기의 단위가 "/" 에서 산산조각 나던 문제

`_expand_slash_options()` 가 모든 "/" 를 잘랐습니다. 환경 분야 보기는
단위에 "/" 가 들어갑니다.

```
700㎥/일 미만|700㎥/일 이상|모름
  → ['700㎥', '일 미만', '일 이상', '모름']
30mg/L 이하|30mg/L 초과       → ['30mg', 'L 이하', 'L 초과']
```

사용자가 "일 미만" 을 고르면 그게 **확인된 조건**으로 답변에 들어갑니다.
→ 양옆 중 한쪽에라도 공백이 있는 "/" 만 구분자로 봅니다. 덤으로,
  콜론 없이 한 칸에 몰아넣은 `저장시설 종류|지하매설/지상·옥내/모름` 도
  이제 살려냅니다(예전엔 조각이 2개라 줄째로 버려졌습니다).

### 수정 — "언제 받았나요?" 에 "아니오" 보기가 붙던 문제

`_YESNO_RE` 를 넓히면서 **의문사 질문**까지 걸리게 됐습니다.

```
마지막 누출검사를 언제 받았나요?   ○ 아래 칸에 날짜 입력   ○ 아니오   ○ 모름
검사 종류가 무엇인지               ○ 정기검사   ○ 아니오   ○ 모름
```

하필 이게 우리가 **최우선으로 묻게 만든** 질문입니다. "아니오" 를 고르면
`"마지막 누출검사를 언제 받았나요?: 아니오"` 가 확인된 조건으로 답변
프롬프트에 들어가고, 거기서 다음 검사일을 계산하려 듭니다.
→ 의문사(언제·무엇·어떻게·얼마·몇 …)가 있으면 부정 보기를 붙이지 않습니다.

### 수정 — 답변의 줄이 서로 달라붙던 문제

숫자 뒤 공백을 붙이는 정규식이 `\s` 를 써서 **줄바꿈까지** 먹었습니다.

```
(전) 「…시행규칙」 별표 4
     일반기준에 따라 매 8년마다 …
(후) 「…시행규칙」 별표 4일반기준에 따라 매 8년마다 …
```

근거 두 줄이 한 줄로 뭉개지고, 별표 번호와 수치가 같은 문장에 들어가
**멀쩡한 답변에 별표 경고까지** 떴습니다. → `[ \t]` 로 바꿨습니다.

### 수정 — 모델이 머리표를 굵게 쓰면 답변 서식이 통째로 사라지던 문제

`**【결론】**` 이 오면 화면에서 `**`→`<b>` 가 먼저 돌아 `<b>【결론】</b>` 이
되고, 소제목 정규식 `^【…】$` 가 안 걸려 **결론 상자와 모든 소제목이
사라졌습니다.** → 판정 전에 머리표를 감싼 굵게 표시를 벗겨냅니다.

### 수정 — "모르겠습니다" 보기가 삭제되던 문제

`_WISH_TAIL_RE`(하소연 걸러내기)가 보기에도 적용되어 "모르겠습니다" 가
**할 일 문장으로 판정돼 삭제**됐습니다. 그러면 "모름" 계열 보기가 하나도
안 남는데, 화면은 그럴 때 **마지막 보기를 기본 선택**합니다.
→ 사용자가 그냥 "이대로 검색" 을 누르면 **모르는 사실을 확정 조건으로**
  넣게 됩니다. 무응답이 오답으로 둔갑하는 셈이라 조용하지만 위험합니다.

### 수정 — 별표 경고 판정 두 건

1. **수치와 유보가 한 문장에 있으면 경고가 안 떴습니다.**
   우리가 프롬프트로 "원문을 확인해야 한다" 고 시켜 놓았기 때문에 모델이
   둘을 같이 씁니다: "별표 4에 따라 **5년마다** 실시하나, 정확한 주기는
   원문을 확인하십시오." → 경고 없이 "5년" 이 화면에 남았습니다.
   → 수치가 있으면 유보 문구가 있어도 경고합니다. 수치가 다음 문장으로
     넘어간 경우("…별표 4에서 정합니다. 그 주기는 8년입니다.")도 봅니다.
2. **수치 판정 정규식이 "별표 4 원문이" 의 `4 원` 을 금액으로 읽었습니다.**
   한 글자 단위(원·일·분·배·주…)는 다른 낱말의 첫 글자이기도 합니다.
   → 여러 글자 단위는 그대로, 한 글자 단위는 뒤가 한글이 아니거나
     조사·수식어로 이어질 때만 인정합니다("8년마다" ○ / "별표 4 원문이" ✗).

### 수정 — 다른 법령의 별표를 자기 것으로 끌어오던 문제

별표 번호를 **인용한 조문이 속한 법령**에 무조건 붙였습니다. 그런데 조문은
다른 법령의 별표도 인용합니다. 「토양환경보전법 시행규칙」 안의
"「폐기물관리법 시행규칙」 별표 5에 따른 …" 이 그 예입니다.
→ 엉뚱한 별표를 AI 에 넣거나, 없으면 "「토양환경보전법 시행규칙」 별표 5 를
  못 받았다" 는 **존재하지도 않는 별표에 대한 경고**를 띄웠습니다.
→ 별표 앞 100자 안의 「법령명」을 보고 주인을 정합니다.

### 수정 — 「…시행규칙」 뒤의 "시행령 제8조" 가 시행규칙으로 붙던 문제

법령명 없이 단계만 쓴 인용을 풀 때, 기준 법령이 이미 하위법령이면
접미사를 안 떼고 덧붙이기만 해서 "…시행규칙시행령" 이라는 없는 이름을
찾다가 실패하고 기준 법령을 그대로 돌려줬습니다.

```
근거: 「토양환경보전법 시행규칙」 제12조제2항
설명: … 자세한 기준은 시행령 제8조에서 정합니다.
  → 시행규칙 제8조(검사기관) 가 근거 카드로 표시됨
```

축약형("영 제8조")은 이미 접미사를 떼고 있었는데, **풀어 쓴 형태가 더
나빴습니다.** 같은 방식으로 떼어냅니다.

### 수정 — law_client 세 건

- **별표번호를 `lstrip("0")` 로 잘랐습니다.** 같은 파일 `_byl_no()` 는 6자리
  0패딩(`000400` = 별표 4)을 제대로 풀면서 "lstrip 으로 자르면 400 이 된다"
  고 경고까지 해 뒀는데, `_extract_articles()` 만 그 경고를 밟고 있었습니다.
  제목이 "[별표 400]" 이 되면 본문의 "별표 4" 와 못 맞춰서 **이미 받아 온
  별표를 "못 받았다" 고 판정**하고 수치를 버립니다. 가지번호도 안 읽어
  별표 6 과 별표 6의3 이 둘 다 "[별표 6]" 이었습니다.
- **`search_all()` 무한 루프.** 종료 조건 세 개가 서로 다른 개수를 봤습니다.
  "100건이 전부 연혁본" 인 페이지가 오면 같은 요청을 끝없이 반복했습니다
  (실측 501회 호출). 아직 호출부가 없어 사용자 피해는 없었지만, 붙이는
  순간 터집니다. → 페이지 상한 + 진전 없으면 중단 + 중복 제거.
- **`get_abbreviations()`** 가 법령명 태그를 `법령명한글` 하나에만 걸고
  폴백이 없었습니다(이 파일의 1순위는 밑줄 있는 `법령명_한글` 입니다).
  태그가 다르면 표가 통째로 비고 약칭 복구가 조용한 no-op 이 됩니다 —
  `현행연혁구분` 사고와 정확히 같은 유형. 또 **한 번의 일시적 실패로 만들어진
  빈 표가 영구 캐시**돼서 재시작 전까지 복구가 안 됐습니다.

### 수정 — 화면 서식 네 건

- **날짜가 두 줄로 쪼개졌습니다.** "이 고시는 2017. 12. 26. 개정된…" 이
  "…2017. 12." / "26. 개정된…" 으로 갈려 시행일을 잘못 읽게 됩니다.
  호(號) 줄바꿈 규칙이 날짜의 마지막 조각을 호 번호로 봤습니다.
- **닫히지 않은 "&lt;" 뒤를 문자열 끝까지 지웠습니다.** "1,000리터 이상
  &lt;2,000리터 미만인 시설" 에서 조건의 상한이 통째로 사라졌습니다.
  → 연혁 표기(개정·신설·삭제…)로 보일 때만 지웁니다.
- **법령 링크 이중 이스케이프.** "「가스 & 석유 사업법」" 이 화면에
  "가스 &amp; 석유 사업법" 으로 보이고 링크도 404 였습니다.
- **【결론】 뒤에 개행이 없으면** 상자도 안 생기고 소제목 규칙에도 안 걸려
  "【결론】" 이 날것으로 노출됐습니다.

### 수정 — 잡다한 것

- 로그인 비밀번호 비교가 **비ASCII 에서 500** 을 냈습니다
  (`hmac.compare_digest` 는 한글 문자열을 못 받습니다). 바이트로 비교합니다.
- 되묻기 보기가 잘린 조각("…확인해야 하")으로 남던 경로를 막았습니다.
  마지막 검사 그물을 루프 밖으로 빼서 **모든 보정 경로**가 통과하게 했습니다.
- 모델이 마크다운 표로 답할 때 정렬 행(`|:---|:---:|`)이 질문·보기로
  올라가던 것을 걸러냅니다.
- "누출검사방법" 처럼 "…방법" 으로 끝나는 용어를 법령명으로 잘못 승격시켜
  헛 조회하던 것을 막았습니다.
- 별표를 캐시된 법령 dict 에 그냥 append 해서 되묻기 라운드마다 같은 별표가
  쌓이던 것을 막았습니다.

---

## v1.27 — 2026-08-18

법제처 OPEN API 활용가이드 매뉴얼 13종(법령 본문 6 + 조항호목·위임·체계도·약칭 6
+ 행정규칙 본문 1)을 `law_client.py` 전체와 대조한 결과를 반영했습니다.
매뉴얼 원본은 `open.law.go.kr\` 폴더에 있습니다.

### 수정 — 멀쩡한 답변에 "확인되지 않은 근거" 경고가 뜨던 문제 (오탐)

증상: 답변이 우리가 시킨 대로

> 「특정토양오염관리대상시설 관리지침」 **별표 4 원문이 제공되지 않아**
> 정기검사 주기를 **확정할 수 없습니다.**

라고 **부정문으로** 잘 썼는데, 화면 맨 위에 노란 경고 상자가 떴습니다.
답이 틀린 것처럼 보여서 담당자가 멀쩡한 답변을 못 믿게 됩니다.

원인은 판정이 너무 거칠었습니다. 본문 전체에서 `별표\s*제?\s*(\d+)` 를
찾기만 하면 "근거로 들었다" 고 봤습니다. 부정문이든 긍정문이든 똑같이요.

→ `_bp_used_as_basis()` 로 **문장 단위** 판정으로 바꿨습니다.
  별표 번호가 나온 문장이

| 문장 | 판정 |
|---|---|
| "별표 4 원문이 제공되지 않아 확정할 수 없습니다" | 경고 안 함 — 오히려 잘 쓴 문장 |
| "별표 1 검토기준에 따라 검토합니다" | 경고 안 함 — 틀릴 숫자가 없음 |
| **"별표 4에 따라 매 8년마다 받아야 합니다"** | **경고함** — 근거 없는 수치 |

즉 **번호와 수치가 같은 문장에 있을 때만** 경고합니다. 이게 원래 이 경고의
목적이었습니다(담당자가 민원 안내에 쓸 숫자가 근거 없이 나온 경우).

### 변경 — 되묻기·답변 프롬프트에 "기준일" 개념 도입

외부 LLM(ChatGPT)이 같은 질문에 이런 답을 냈다는 지적에서 출발했습니다.

> 2025년 9월 누출검사를 받은 30,000L 주유소 지하저장시설의 다음 정기
> 누출검사는 **2033년**에 실시해야 합니다.

우리 앱은 되묻기에서 **설치 경과연수**(10년 미만/이상)만 물었지, **마지막
검사일**은 한 번도 묻지 않았습니다. 그러니 애초에 날짜를 계산할 재료가
없었습니다. 답변 프롬프트에도 계산 지시가 없었습니다.

**되묻기(`CLARIFY_PROMPT`)에 추가**

- 무엇을 먼저 물을지 순서를 못박음: **A 적용대상 → B 의무발생 → C 주기·기한
  → D 예외**. 가장 결정적인 것을 맨 앞 줄에 둔다.
- 기간을 묻는 질문("언제까지 / 몇 년마다 / 다음 검사는 언제")이면 확인할 것
  다섯 가지(대상 여부, 검사 종류, 최초/정기, **직전 검사일**, 기준일).
- ★ **"설치한 지 15년 됐다" 와 "2025년 9월에 검사받았다" 는 완전히 다른
  정보다** — 설치 경과연수는 *대상 여부*를, 마지막 검사일은 *다음 검사일*을
  가릅니다. 같은 것으로 취급하지 말라고 명시.
- 날짜는 보기로 만들 수 없으므로 화면의 자유 입력 칸을 쓰도록 안내:
  `마지막 누출검사를 언제 받았습니까?|아래 칸에 날짜 입력|받은 적 없음|모름`
- 한 라운드 질문 수 **6개 → 3개**. 많이 묻는 것보다 결정적인 것을 먼저.
- "예/아니오로 만들어라" 규칙을 "한쪽만 주지 말고 반대쪽도 넣어라" 로 완화.
  코드가 이미 부정 보기를 채우므로, 구체적인 말("석유류")을 살리는 쪽이 낫습니다.

**답변(`ANSWER_PROMPT`)에 추가**

- `━━ 기간 계산 ━━` 절 신설. 조문이 "설치한 날부터" 인지 "직전 검사를 받은
  날부터" 인지 보고 기준일을 잡을 것. 기준일과 주기가 다 있으면 **날짜를
  계산해서 말할 것.** 기준일을 모르면 계산하지 말고 **무엇을 알려주면
  계산할 수 있는지 밝힐 것.**
- **【적용 조건】** 블록 — 사용자가 알려준 사실만 한 줄씩(시설/용량/설치 경과/
  검사 종류/마지막 검사). 안 알려준 항목은 줄째로 뺍니다. "미확인" 을 줄줄이
  적으면 화면만 길어집니다.
- **【계산】** 블록 — `기준일 + 주기 = 다음 시점`. **기준일과 주기가 둘 다
  확인될 때만** 씁니다.
- 【근거】를 법률 → 시행령 → 시행규칙 → 별표 순으로 적도록 명시.

**일부러 반영하지 않은 것**

| 제안 | 안 쓴 이유 |
|---|---|
| JSON 출력(`{"need_clarify": ...}`) | 9B 로컬 모델이 JSON 을 자주 깨뜨립니다. 지금의 `질문\|보기\|보기` 가 더 안전하고, 이미 파서 보정을 여러 겹 쌓아 뒀습니다. |
| `## 결론` / `### 1. 법률` 마크다운 제목 | 화면과 PDF 가 `【 】` 를 소제목으로 인식합니다. `#` 은 출력 정리 단계에서 제거됩니다. 형식만 바꾸면 렌더링이 통째로 깨집니다. |
| "질문은 하나만" | 되묻기 한 라운드마다 **검색 전체가 다시 돕니다**(약 16초, API 14회). 1개씩 6라운드면 1분 반이 검색에만 듭니다. 3개로 절충했습니다. |
| 부칙·경과조치 검토 | 지금 **부칙을 아예 수집하지 않습니다.** 없는 자료를 보라고 시키면 지어내거나 "확인 불가" 만 늘어납니다. 부칙 수집을 먼저 붙여야 의미가 있습니다. |
| STEP 1~9 절차 나열 | 프롬프트가 길어질수록 9B 모델의 형식 이탈이 늘어납니다. 실제 판단이 갈리는 규칙만 넣었습니다. |

★ **다만 이 변경만으로 "2033년" 이 나오지는 않습니다.** 주기 **8년** 은
「…관리지침」 별표 4 에 있는데 그 별표 본문을 아직 못 받고 있습니다. 계산
지시가 있어도 곱할 숫자가 없습니다. 외부 LLM 이 답할 수 있었던 것은
프롬프트가 좋아서가 아니라 **그 숫자를 이미 알고 있었기 때문**입니다.
별표 본문 확보(`dump_byeolpyo.py` 실측)가 이 격차의 진짜 해결책입니다.

### 변경 — 되묻기·답변 프롬프트 개정 (기간 계산을 실제로 하도록)

계기: 같은 질문("누출검사주기가 어떻게 되나요? 주유소이고 지하 저장,
설치한 지 15년")을 ChatGPT 에 물었더니 **"2033년에 실시해야 합니다"** 라고
날짜까지 계산해서 답했습니다. 우리 앱은 주기 얘기만 하고 끝났습니다.

원인을 갈라 보면 두 가지인데, **하나는 프롬프트, 하나는 데이터**입니다.

**(1) 프롬프트 — 고칠 수 있는 것.**
우리 되묻기는 "설치한 지 몇 년인지" 는 물어도 **"마지막 검사를 언제 받았는지"
는 한 번도 묻지 않았습니다.** 그런데 다음 정기검사일은 설치일이 아니라
**직전 검사일**로 계산합니다. 물어보지 않으니 계산할 재료가 없었습니다.

→ `CLARIFY_PROMPT` 에 넣은 것:
  - **무엇을 먼저 물을지 순서** — A 적용대상 → B 의무 발생 → C 주기·기한 →
    D 예외. A 가 비어 있으면 A 부터 묻고, 가장 결정적인 것을 맨 앞 줄에.
  - **날짜·주기 질문 특별 규칙** — "언제까지 / 몇 년마다 / 다음 검사는 언제"
    를 물으면 ① 대상인지 ② 검사 종류 ③ 최초/정기 ④ **직전 검사일**
    ⑤ 기준일을 확인한다.
  - ★ **"설치한 지 15년" 과 "2025년 9월에 검사받았다" 는 다른 정보다.**
    전자는 대상 여부, 후자는 다음 검사일 계산에 쓴다. 같은 것으로 취급 금지.
  - 날짜는 보기로 못 만드니 `아래 칸에 날짜 입력|받은 적 없음|모름` 형태로
    묻고 화면의 자유 입력 칸을 쓰게 했습니다.
  - 한 번에 묻는 개수를 6개 → **3개**로 줄였습니다. 많이 묻는 것보다
    결정적인 것을 먼저 묻는 편이 낫고, 라운드마다 전체 재검색이 도는
    현재 구조에서는 질문이 많을수록 화면이 지저분해집니다.

→ `ANSWER_PROMPT` 에 넣은 것:
  - **【적용 조건】 블록** — 시설/용량/설치 경과/검사 종류/마지막 검사 중
    사용자가 알려준 것만 한 줄씩. 없으면 블록째 생략(빈 칸을 "미확인" 으로
    채우면 화면만 길어집니다).
  - **【계산】 블록** — 기준일과 주기가 **둘 다 확인될 때만** 씁니다.
    `기준일 2025-09-15 + 주기 8년 = 2033-09-15` 처럼 근거와 계산을 분리.
  - **기간 계산 규칙** — "설치 후 몇 년" 과 "마지막 검사 후 몇 년" 을 구분하고,
    조문이 어느 날을 기준으로 세라고 하는지 확인한 뒤 그 날을 기준일로 삼는다.
    기준일을 모르면 계산하지 말고 **무엇을 알려주면 계산되는지** 밝힌다.
    주기 수치가 [조문 원문]에 없으면(별표가 안 왔으면) 지어내서 계산 금지.

**받아들이지 않은 것** (그대로 넣으면 깨집니다):

| 제안 | 왜 안 썼는지 |
|---|---|
| JSON 출력(`{"need_clarify": …}`) | 파서가 `질문\|보기\|보기` 형식 기준입니다. 9B 로컬 모델은 지금도 파이프 형식을 자주 어기는데 JSON 은 더 어렵습니다. 형식 사고가 늘어납니다 |
| `## 결론` / `### 1. 법률` 마크다운 제목 | 화면은 `【…】` 를 소제목으로 렌더합니다. 게다가 `_clean_output()` 이 `#` 로 시작하는 줄을 제거해서 제목이 통째로 사라집니다 |
| "질문은 하나만" | 되묻기 라운드마다 조문 수집이 통째로 다시 돕니다(약 16초). 1개씩 6라운드면 체감이 너무 느립니다. 3개로 절충 |
| 부칙 확인 지시 | **부칙을 아직 수집하지 않습니다.** 없는 자료를 확인하라고 시키면 지어내거나 "확인 불가" 소리만 늘어납니다. 부칙 수집을 붙인 뒤에 넣을 항목입니다 |
| STEP 1~9 절차 나열 | 프롬프트가 길어질수록 로컬 모델의 형식 이탈이 늘어납니다. 실제 판단에 쓰이는 규칙만 남겼습니다 |

**(2) 데이터 — 프롬프트로는 못 고치는 것.**
ChatGPT 가 "8년" 을 말할 수 있었던 건 프롬프트가 좋아서가 아니라 **별표 본문을
이미 알고 있었기** 때문입니다. 우리는 「특정토양오염관리대상시설 관리지침」
별표 4 본문을 법제처 API 로 못 받고 있고, 그래서 (올바르게) "확정할 수
없습니다" 라고 답합니다. 프롬프트를 아무리 다듬어도 없는 숫자는 안 나옵니다.
→ `dump_byeolpyo.py` 실측으로 별표 본문 확보 경로를 찾는 것이 **다음 순서**입니다.

### 수정 — 되묻기 보기가 "…확인이 필요합니다" 로 나오던 문제

실사용 지적: "첫 번째 되묻기인데 기존이랑 내용이 너무 달라졌다."

모델이 낸 줄:

```
질문|누출검사 주기가 달라지는 시설 규모나 용량 기준이 궁금합니다.
    |소유주명의로 등록한지 10년 미만인지 10년 이상인지 확인이 필요합니다.
    |시설 내 저장탱크의 총 용량이 30m³ 미만인지 30m³ 이상인지 확인이 필요합니다.
    |저장된 물질이 석유류인지 유해화학물질인지 확인이 필요합니다.
```

질문 자리에는 하소연("…궁금합니다")이, 보기 자리에는 **할 일 세 개**가
들어왔습니다. 화면은 이렇게 됩니다 — 무엇을 고르라는 건지 알 수 없습니다.

```
누출검사 주기가 달라지는 시설 규모나 용량 기준이 궁금합니다.
  ○ …확인이 필요합니다.   ○ …확인이 필요합니다.   ○ …확인이 필요합니다.
```

프롬프트에는 이미 **"'확인 필요', '검토 요망', '여부 확인' 같은 할 일을 보기로
만들지 마라"** 고 적혀 있습니다. 로컬 모델이 그냥 무시한 것이고, 코드에는
막는 장치가 없었습니다. 기존 보정들은 전부 **물음 어미**를 보고 판정하는데
"…확인이 필요합니다" 는 물음이 아니라서 그물을 그대로 통과했습니다.

→ 그런데 보기 하나하나가 **그 자체로 멀쩡한 질문**입니다("A인지 B인지").
  `_promote_todo_options()` 로 군말을 떼고 **각각을 질문으로 세웁니다.**

```
소유주명의로 등록한지 10년 미만인지 10년 이상인지
  ○ 10년 미만    ○ 10년 이상        ○ 모름
시설 내 저장탱크의 총 용량이 30m³ 미만인지 30m³ 이상인지
  ○ 30m³ 미만    ○ 30m³ 이상        ○ 모름
저장된 물질이 석유류인지 유해화학물질인지
  ○ 석유류       ○ 유해화학물질     ○ 모름
```

앞쪽 군말은 짝을 맞춰 잘라냅니다(`_trim_parallel`) — "소유주명의로 등록한지
10년 미만" / "10년 이상" 처럼 한쪽만 길어지면 고르기 어렵습니다.

→ 마지막 그물도 넣었습니다. 여기까지 와서도 "…확인이 필요합니다" 가 남은
  보기는 **화면에 안 내보냅니다.** 고를 수 없는 보기를 보여 주느니 그 질문을
  안 묻는 편이 낫습니다.

### 수정 — 되묻기 보기가 "예 / 아니오" 로 **갈아치워지던** 문제

실사용 지적: "두 번째 되묻기에 **예** 라고 글자가 잘못 들어간다."

확인해 보니 그 "예" 는 모델이 낸 것이 아니라 **우리 코드가 집어넣은 것**
이었습니다. `_fix_yesno()` 가 이렇게 돼 있었습니다.

```python
# 실질 보기가 하나뿐 = 부정을 고를 수 없는 상태입니다.
return ["예", "아니오", "모름"]
```

모델이 `저장된 물질이 석유류인가요?|석유류|모름` 을 냈을 때, 부정 보기가
없다는 이유로 **보기 전체를 통째로 갈아치웠습니다.**

```
(전) 저장된 물질이 석유류인가요?   ○ 예       ○ 아니오   ○ 모름
(후) 저장된 물질이 석유류인가요?   ○ 석유류   ○ 아니오   ○ 모름
```

모델이 애써 뽑아 준 "석유류" 라는 구체적인 말이 사라져서, 담당자가 질문을
다시 읽어야 무엇에 "예" 인지 알 수 있었습니다. 정보가 줄어드는 수정이었습니다.

→ 있는 보기는 **그대로 두고 부정만 채웁니다.** 실질 보기가 아예 없을 때만
  예/아니오로 세웁니다.

### 수정 — "지정되었나요?" 에 부정 보기가 안 붙던 문제

`_YESNO_RE` 어미 목록에 `되었나요`·`했나요`·`받았나요`·`인가` 가 빠져 있었습니다.
그래서 이런 화면이 나갔습니다 — **아니라고 답할 방법이 없습니다.**

```
시설이 특정토양오염관리대상시설로 지정되었나요?   ○ 지정됨   ○ 모름
```

→ 어미를 보강했습니다. 이제 `○ 지정됨  ○ 아니오  ○ 모름` 으로 나옵니다.

### 수정 — 되묻기의 질문과 보기가 **둘 다 물음** 으로 나오던 문제

실제 사례 (2026-08-18 로그):

```
질문|주유소 지하매설 저장시설인가?|지상·옥내 저장시설인가?|모름
```

모델이 서로 배타적인 두 상태를 각각 **물음으로** 써 놓았습니다. 화면은
이렇게 되어 첫 번째를 고를 방법이 없습니다.

```
주유소 지하매설 저장시설인가?
  ○ 지상·옥내 저장시설인가?   ○ 모름
```

기존 `_repair_a_or_b()` 는 보기 자리의 질문을 **버리는** 방향이라 이 줄에서는
살릴 수 있는 선택지가 먼저 사라졌습니다.

→ `_repair_all_questions()` 를 앞단에 넣었습니다. 물음 어미를 떼어 **양쪽 다
  보기로** 세우고, 공통 꼬리말로 질문을 새로 만듭니다.

```
저장시설 중 어느 쪽입니까?
  ○ 주유소 지하매설 저장시설   ○ 지상·옥내 저장시설   ○ 모름
```

→ 보기가 물음인지 판정하는 `_OPT_LOOKS_LIKE_Q_RE` 에 "인가/인지/는가" 를
  **물음표가 붙었을 때만** 추가했습니다(`_OPT_Q_MARK_RE`). 물음표 없이
  어미만 보면 "이중벽인지", "2만리터 이상인지" 같은 멀쩡한 보기까지 버려집니다
  — 예전에 실제로 그 사고가 났었습니다.

회귀 확인(6가지 형태 모두 정상):

| 입력 | 결과 |
|---|---|
| `A인가?\|B인가?\|모름` | **재구성** → A / B / 모름 |
| `A인지 B인지\|C인지 D인지\|모름` | 기존 경로 유지 |
| `용량이 2만리터 이상인지\|2만리터 이상\|2만리터 미만` | 그대로 (오탐 없음) |
| `…에 해당하나요?\|방사성폐기물\|모름` | 예 / 아니오 / 모름 |
| `용량 범위\|A / B / 모름\|모름` | "/" 펴기 유지 |
| `이중벽 구조인가?\|이중벽\|단일벽` | 그대로 (오탐 없음) |

### 수정 — 답변 강조(**굵게** / __밑줄__)가 화면에 한 번도 안 나오던 문제

증상: "중요한 내용은 굵게/밑줄로 표시해 달라" 고 v1.7 부터 넣어 뒀는데, 어느
순간부터 답변이 전부 민무늬 글자로만 나왔습니다.

원인은 프롬프트도 화면도 아니고 **그 사이**였습니다. `ai_client._clean_output()`
에 이 한 줄이 있었습니다.

```python
# 마크다운 강조 (**법령명:** 처럼 라벨을 감싸는 경우)
t = re.sub(r"\*\*(.+?)\*\*", r"\1", t)
```

원래는 용어추출·되묻기 단계에서 모델이 `**법령명:**` 처럼 라벨을 감싸 파싱이
깨지는 걸 막으려던 것인데, 이 함수는 **모든 LLM 응답**을 지나갑니다. 답변도
예외가 아니라서:

- 프롬프트: "수치는 `**굵게**`, 의무는 `__밑줄__`" ← 잘 지시하고
- 모델: 실제로 `**10년**` 이라고 출력하고
- `_clean_output()`: **여기서 마커를 지우고**
- 화면: `**`→`<b>` 변환 규칙이 멀쩡히 있는데 지울 게 없음

이 순서로 조용히 사라지고 있었습니다. 같은 줄에서 `^\s*[#>\-\*]+\s*` 도
돌아서 **중첩 글머리표(`- 지하매설이면 …`)까지 납작하게** 눌렸습니다.

→ 응답이 **답변인지 아닌지**를 【근거】/【설명】/【결론】 머리표로 구분합니다.
  답변이면 강조와 `-` 글머리표를 살리고, 파싱 대상 응답(용어·되묻기·조문선택)
  에서만 예전처럼 전부 걷어냅니다.
→ 답변 쪽에서는 `제**12**조` 처럼 조문번호에 낀 굵게만 따로 제거합니다.
  그대로 두면 화면의 조문 링크 정규식(`제\d+조`)이 못 읽어 링크가 사라집니다.
→ `.answer b.num` / `u.duty` / `mark.key` CSS 가 **아예 없었습니다**(인쇄용
  오버라이드만 있었음). 굵게는 연노랑 배경, 밑줄은 금색 실선으로 넣었습니다.

### 추가 — 【결론】 — 답변 맨 위에 "그래서 뭘 하면 되는지"

답변이 【근거】 → 【설명】 순서라, 담당자가 민원인에게 바로 말할 한 줄을
찾으려면 설명 4단계를 다 읽어야 했습니다.

→ 답변 형식 맨 앞에 **【결론】** 을 넣었습니다. 2~3줄로 질문에 직접 답하고,
  조문번호는 나열하지 않습니다. 조건이 "모름" 이면 "○○이면 A, □□이면 B" 로
  씁니다. 여기에도 수치는 굵게, 의무는 밑줄을 씁니다.
→ 화면에서는 파란 테두리 상자(`.concl`)로 답변 맨 위에 띄웁니다.
→ PDF 저장은 `pdf_maker.py` 가 `【…】` 를 이미 일반 규칙으로 처리하고 있어
  따로 손대지 않아도 소제목으로 나옵니다.

예시 문구:

```
【결론】
설치 전에 __관할 시장·군수·구청장에게 신고해야 합니다__.
변경 시에는 **30일 이내** 신고입니다.
```

### 수정 — 위임법령 힌트가 **한 건도** 안 붙던 문제 (심각) — 실측으로 원인 확정

증상: v1.26 에서 붙인 위임법령 힌트가 실사용에서 전혀 안 나왔습니다.
처리 과정에 "위임법령 힌트 추가" 단계가 안 뜨고, 근거 원문에도 힌트 문장이
없었습니다. 서버 로그에는 `target=lsDelegated&ID=000160` 호출이 `200 OK` 로
찍히는데도 그랬습니다 — **API 는 성공했고 파서가 전부 버리고 있었습니다.**

원인이 두 겹이었습니다.

**(1) 응답 구조를 잘못 알고 있었습니다 (진짜 원인).**
가이드 PDF 에 XML 예시가 없어서 필드 목록만 보고 "한 엘리먼트 안에 조문번호와
위임법령조문번호가 나란히 있겠거니" 하고 짰는데, `/api/raw` 로 원본을 받아 보니
**3단 중첩**이었습니다. 두 필드가 아예 다른 형제 가지에 있어서, 어떤 노드도
조건을 만족하지 못해 **항상 빈 리스트**였습니다.

```xml
<lsDelegated><법령>
  <위임조문정보>                       ← 법률 조문 1개당 1블록
    <조정보>                           ← ★ 법률 쪽 조문번호는 여기
      <조문번호>4</조문번호>
      <조문가지번호>2</조문가지번호>    (가지조문일 때만)
      <조문제목>토양오염의 우려기준</조문제목>
    </조정보>
    <위임정보>                          ← 여러 개 반복
      <위임구분>시행령</위임구분>
      <위임법령일련번호>284891</위임법령일련번호>
      <위임법령제목>토양환경보전법 시행령</위임법령제목>
      <위임법령조문정보>                ← 여러 개 반복. ★ 대상 조문번호는 여기
        <위임법령조문번호>6</위임법령조문번호>
        <위임법령조문가지번호>2</위임법령조문가지번호>   (있을 때만)
        <조항호목>제12조제1항</조항호목>  ← 법률 쪽 몇 항/호에서 위임했는지
      </위임법령조문정보>
    </위임정보>
    <위임정보>                          ← 위임행정규칙은 모양이 또 다릅니다
      <위임구분>위임행정규칙</위임구분>
      <위임행정규칙조문정보>            ← 제목·일련번호가 ★이 안에★ 있고
        <위임행정규칙일련번호>2100000072350</위임행정규칙일련번호>
        <위임행정규칙제목>토양지하수정보시스템 … 규정</위임행정규칙제목>
        <조항호목>제4조의3제2항</조항호목>
      </위임행정규칙조문정보>            ← 조문번호는 아예 안 옵니다
    </위임정보>
  </위임조문정보>
</법령></lsDelegated>
```

→ `<위임조문정보>` 를 블록 단위로 돌면서 `<조정보>`(법률 쪽)와
  `<위임정보>/*조문정보`(위임 대상)를 짝지어 평평한 레코드로 펴도록
  **파서를 다시 썼습니다.**

**(2) 위임 대상 종류마다 필드 이름이 다릅니다.**

| 위임구분 | 제목 필드 | 조문번호 |
|---|---|---|
| 시행령 · 시행규칙 · 인용법령 | `위임법령제목` | `위임법령조문번호` (옴) |
| **위임행정규칙**(고시·훈령·예규) | `위임행정규칙제목` | **없음** |
| 위임자치법규(조례·규칙) | `위임자치법규제목` | **없음** |
| 공공기관 규정 / 조약 | `위임규정제목` / `조약제목` | **없음** |

그래서 구조를 고쳐도, 조문번호가 안 오는 **행정규칙·자치법규 위임은 여전히
버려질 뻔했습니다.** 실무에서 제일 중요한 "법 제N조 → 어느 고시" 연결입니다.

→ 종류를 `_kind` / `_kind_raw` / `_title` / `_seq` / `_jo` / `_jo_gaji` 로
  정규화해서 돌려줍니다. 제목만 있으면 레코드로 인정합니다.
→ `main.py` 힌트 생성부도 함께 고쳤습니다. 조문번호가 없는 종류는
  `「지침명」(위임행정규칙, 조문번호 미제공)` 으로 표시하고, AI 에게 "조문번호를
  인용하지 말고 이름만 쓰라" 고 못박습니다. (없는 조문번호를 지어내는 것을 막음)

**실측 결과 (토양환경보전법 ID=000160):**

| | 건수 |
|---|---|
| `<위임조문정보>` 블록 (= 위임이 걸린 법률 조문) | 55 |
| `<위임정보>` | 102 |
| 정리 후 최종 레코드 | 217 |
| └ 인용법령 / 시행령 / 시행규칙 / 위임행정규칙 | 137 / 37 / 35 / 8 |

실제로 만들어지는 힌트 문장(법 제12조):

```
(※ 법제처 위임법령 데이터: 이 조문은 「토양환경보전법 시행령」 제6조,
「토양환경보전법 시행령」 제7조에 위임됩니다. … 또한 법제처 데이터상 이 조문이
참조하는 조문: 「위험물안전관리법」(인용법령, 조문번호 미제공) — 이것은 위임이
아니라 상호참조이므로 '하위법령' 이라고 쓰지 마십시오.)
```

★ 이 힌트가 겨냥하는 실제 오류가 직전 테스트에서 그대로 나왔습니다.
질문(주유소 특정토양오염관리대상시설 신고)에 대해 AI 가
**「토양환경보전법」 제6조제1항** 이라고 답했는데, 그 내용(도면·위험물안전관리법
설치허가서 첨부)은 **시행령 제6조** 의 것입니다. 위임 데이터상
`법 제12조제1항 → 시행령 제6조` 가 정답입니다. 법률과 시행령의 같은 조번호를
헷갈린, 정확히 이 힌트가 막으려던 유형입니다.

### 수정 — 인용법령(상호참조)을 위임과 섞어 쓰던 문제

`위임구분` 값에 `인용법령` 이 있는데, 이건 위임이 아니라 단순 상호참조입니다.
그런데 **건수가 압도적**입니다 (실측 217건 중 137건). 한 덩어리로 묶어서
"…에 위임됩니다" 라고 넣으면 두 가지가 동시에 깨집니다.

- 조문당 앞에서 몇 개만 잘라 쓰므로, 정작 필요한 시행령·시행규칙이 밀려서
  힌트에 안 들어갑니다.
- AI 가 상호참조를 하위법령으로 오해합니다.

→ `_rank` 로 **시행령 → 시행규칙 → 위임행정규칙 → 자치법규 → 인용법령** 순으로
  정렬합니다. `delegation_map()` 도 이 순서를 유지합니다.
→ `main.py` 는 위임(최대 4건)과 인용(최대 3건)을 **문장을 나눠서** 넣고,
  인용 쪽에는 "위임이 아니라 상호참조" 라고 명시합니다.

### 수정 — 같은 위임이 항·호마다 중복되던 문제

`제12조제1항 → 시행령 제6조`, `제12조제2항 → 시행령 제6조` 처럼 같은 대상이
항·호 수만큼 반복해서 옵니다. 그대로 두면 힌트 4칸이 같은 조문으로 다 찹니다.

→ (대상 법령, 대상 조문번호) 기준으로 합치고, 어느 항·호에서 왔는지는
  `_from` 리스트에 모아 둡니다. (실측 455 → 217건)

### 수정 — 존재하지 않는 "제0조" 를 만들어 내던 문제

인용법령 레코드에 `<위임법령조문번호>0</위임법령조문번호>` 이 옵니다
(실측: 법 제12조 → 「위험물안전관리법」). "법률 전체를 가리킴" 이라는 뜻인데
그대로 쓰면 **「위험물안전관리법」 제0조** 라는 없는 인용이 만들어집니다.

→ `0` / `000` 은 빈 값으로 눕혀서 "조문번호 미제공" 으로 표시합니다.

### 수정 — 가지조문에 엉뚱한 위임 정보가 붙던 문제

`delegation_map()` 은 `(조문번호, 조문가지번호)` 로 묶습니다. 구조를 잘못
알던 때는 가지번호를 `조항호목` 문자열에서 정규식으로 되살리려 했는데,
실측해 보니 **`<조정보>` 안에 `조문가지번호` 가 그냥 들어 있습니다.**
정규식 복원은 걷어내고 원본 필드를 직접 읽습니다.

→ 조문번호가 본문 조회에서는 `53`, `lsDelegated` 에서는 `0053` 으로 올 수
  있으므로 양쪽 키를 모두 등록해 호출부가 어느 형식으로 찾아도 걸리게 했습니다.
  (가지번호도 동일)

### 추가 — 위임된 하위법령 조문만 골라 받기 (`JO` 파라미터)

`lsDelegated` 가 "법 제53조 → 시행령 **제72조**" 를 조문번호까지 알려주는데,
그 제72조를 확인하려고 지금까지 **시행령 전체**를 받았습니다. 큰 시행령은
수백 KB 라 AI 컨텍스트가 순식간에 찼습니다.

매뉴얼 '현행법령 본문 조회' 의 `JO` 파라미터를 씁니다.
`JO` = 6자리 = 조번호(4) + 조가지번호(2) — `000200`=제2조, `001002`=제10조의2.

- `_jo_code(no, gaji)` — 입력이 `"2"` / `"0072"` / `"007200"` / `"제72조"` 로
  제각각 와도 6자리로 정규화합니다.
- `get_article(target, law_id/mst, jo_no, jo_gaji, ef_yd)` — 특정 조문만 조회.
  ★ `target=eflaw` 를 **MST 로** 조회할 때는 매뉴얼상 `efYd`(시행일자)가
  **필수**입니다. 안 주면 예외를 냅니다. `ID` 로 조회하면 현행 본문이 오고
  `efYd` 는 불필요합니다.
- `resolve_current_id(법령명)` — `lsDelegated` 는 위임 대상의 MST 만 주고
  법령ID 를 안 줍니다. 이름으로 현행판 ID 를 되찾습니다. **정확히 같은
  이름만** 받습니다 (부분 일치를 허용하면 시행령을 찾으려다 시행규칙을
  물고 오는 사고가 납니다).
- `get_delegated_articles(law_id, jo_no)` — 위 셋을 잇습니다. 조문번호를
  주지 않는 종류는 `note` 에 이유를 남기고 본문 조회를 건너뜁니다.

### 수정 — 목록 조회 `display` 상한 미검증 / 페이징 없음

- 매뉴얼 `display` 는 `max=100` 입니다. `_clamp_display()` 로 1~100 으로
  조정합니다. 안 그러면 "받은 개수 < display" 로 마지막 페이지를 판정할 때
  조건이 영원히 참이 되지 않습니다.
- `search()` 에 `page` 파라미터를 추가했습니다.
- `search_all()` 추가 — 100건 넘는 결과를 이어 받습니다. 종료 조건을 두 개
  겹쳤습니다: (a) **필터 전** 원본 레코드 수 < per_page, (b) `totalCnt` 도달.
  (a) 를 필터 후 개수로 재면 연혁본이 걸러져 개수가 줄었을 때 뒷페이지가
  남았는데도 끝난 줄 압니다.
- `totalCnt` 를 파싱합니다 (`_parse_search_rows`).

### 수정 — 스펙에 없는 파라미터를 보내던 문제

매뉴얼 대조 결과 `search`(검색범위) 파라미터가 **없는** target 이 있습니다.

- `lsStmd`(체계도 목록), `lsHistory`(연혁 목록), `lsAbrv`(약칭) → `NO_SCOPE_TARGETS`
- `lsAbrv` 는 `page` 도 없습니다 → `NO_PAGE_TARGETS`

해당 target 에는 이제 그 파라미터를 보내지 않습니다.

### 수정 — 법령 연혁(`lsHistory`)은 XML 을 지원하지 않음

매뉴얼: 법령 연혁 목록/본문 조회의 출력 형태는 **HTML 뿐**입니다
(체계도는 HTML/XML/JSON). `_get()` 은 `type=XML` 고정이라 `lsHistory` 는
무조건 `ET.ParseError` 가 났고, "XML 파싱 실패" 라는 엉뚱한 메시지로
흘러갔습니다. `HTML_ONLY_TARGETS` 가드를 넣어 원인을 그대로 말해 줍니다.

### 추가 — `dump_byeolpyo.py` (실측 스크립트)

매뉴얼 '현행법령 본문 조회' 응답 필드 목록에는 `별표번호` / `별표가지번호` /
`별표구분` / `별표제목` / `별표내용` / `별표서식PDF파일링크` 가 **전부**
적혀 있습니다. v1.26 에서 "법령 본문 조회에는 별표가 아예 딸려오지 않는다"
고 단정하고 `get_byeolpyo()` 우회를 만들었는데, 매뉴얼과 어긋납니다.

원본 XML 로 확정하기 위한 스크립트입니다. 표준 라이브러리만 씁니다.

```
python dump_byeolpyo.py
python dump_byeolpyo.py "물환경보전법 시행규칙"
```

`files\_dump\` 에 `eflaw` / `law` / `licbyl` 원본 XML 을 저장하고 콘솔에
판정을 출력합니다. 같이 확인하는 것:

- `.//별표단위` XPath 가 실제 응답에 맞는지
- `별표번호` 원본이 `0004` 인지 `000400` 인지
  → `law_client.py` 의 `_extract_articles()` 는 별표번호에 `lstrip("0")` 을
  쓰는데, 같은 파일 `_byl_no()` 는 "6자리면 `lstrip` 이 틀린다" 고 경고합니다.
  둘 중 어느 쪽이 맞는지 이 실측으로 결정합니다.
- `별표가지번호` 가 따로 오는지 (지금은 안 읽어서 별표 6 과 별표 6의3 이
  둘 다 `[별표 6]` 으로 표기됩니다)
- `별표내용` 에 표 본문이 있는지, 파일 링크만 있는지

### 추가 — `test_law_client_patch.py` (회귀 테스트)

네트워크 없이 도는 단위 테스트 **45건**. 파서·정규화 함수만 검증합니다.

```
python test_law_client_patch.py
```

★ `get_delegated` 픽스처는 **실측 응답 모양**으로 바꿨습니다. 처음엔 매뉴얼
필드 목록만 보고 "평평한 `<조문>`" 으로 지어냈는데, 픽스처가 틀려 있으면
테스트가 통과해도 실사용에서 그대로 깨집니다 — 실제로 그렇게 됐습니다.

### 수정 — `lsAbrv` 30페이지 헛돌기 방지

`get_abbreviations()` 는 `page` 를 1→30 으로 올리며 받는데, 매뉴얼상
`lsAbrv` 의 요청변수는 `OC/target/type/stdDt/endDt` 뿐이라 **`page` 가
없습니다.** 서버가 무시하면 1페이지를 30번 받게 됩니다.

→ 새로 들어온 약칭이 하나도 없으면 거기서 끊습니다. `page` 를 지원하면 이
  조건은 안 걸리므로 양쪽 다 안전합니다.

### 남은 것 (다음 작업)

| 항목 | 상태 |
|---|---|
| `_extract_articles()` 별표번호 `lstrip("0")` + 가지번호 미표기 | `dump_byeolpyo.py` 실측 후 |
| `admrul` ID 폴백이 `ID`/`LID` 를 혼동 (매뉴얼: `ID`=일련번호, `LID`=행정규칙ID) | 미수정 |
| 법령 본문에 별표가 오는지 | 실측 대기 |
| `licbyl` 매뉴얼 PDF 없음 — `get_byeolpyo()` 가 전부 실측 추측 | PDF 필요 |
| `부칙` / `개정문내용` / `제개정이유내용` 미수집 | 미착수 |
| `resultCode`(성공 00 / 실패 01) 미검사 | 미착수 |
| `lawjosub` / `eflawjosub` (조항호목 단위 조회) | 보류 — `JO` 조 단위로 충분 |

---

## v1.26 — 2026-08-17

### 수정 — 답변이 만들어졌는데 "서버에 연결하지 못했습니다: question is not defined" 로 뜨던 문제
서버 로그를 보면 조문 검색부터 답변 생성까지 전부 정상 처리됐는데,
화면에는 연결 오류로 표시되는 증상이었습니다.

원인은 `static/index.html` 의 `ask()` 함수입니다. 질문 본문은 `base` 라는
변수에 담아 쓰는데, 답변을 히스토리에 저장하는 부분(`history.unshift({...})`)
한 군데만 존재하지 않는 `question` 변수를 참조하고 있었습니다.
화면에 답변까지 다 그려진 뒤(`renderAnswer`) 이 줄에서 `ReferenceError` 가
터져 `catch` 블록으로 빠지면서 "서버에 연결하지 못했습니다" 로 잘못 표시된
것입니다. 서버(백엔드) 문제가 아니라 화면 쪽 오타였습니다.

→ `q: question` 을 `q: base` 로 수정.

### 수정 — 별표 본문이 없는데 AI 가 검사주기를 지어내던 문제 (심각)
주유소 누출검사주기 질문에서 답변이 이렇게 나왔습니다.

| 회차 | 답변한 주기 | 인용한 곳 |
|---|---|---|
| 1차 | 설치 후 10년부터 **매 8년** | 별표 4 제2호 |
| 2차 | 설치 후 5·10·15년, 이후 **매 2년** | 별표 4 제1호가목 |

**같은 질문에 서로 다른 수치입니다. 둘 다 근거가 없습니다.**

시행규칙 제12조제2항은 "누출검사주기는 별표 4와 같다" 라고만 정합니다.
실제 주기는 별표 4 에 있는데, **법제처 법령 본문 조회(target=law)에는
별표가 아예 딸려오지 않습니다.** (행정규칙 target=admrul 은 함께 옵니다)

기존 "별표 자동 포함" 로직은 이미 받아온 조문 목록 안에서만 별표를 찾으므로,
애초에 안 온 별표는 찾을 수가 없었습니다. 그래서 컨텍스트에 별표가 없는
상태로 답변이 만들어졌고, 모델이 기억으로 숫자를 채웠습니다.

세 겹으로 막았습니다.

**1. 별표·서식 API 로 따로 조회** (`law_client.get_byeolpyo()`)
   본문 조회로 안 잡힌 별표는 별표·서식 목록 API 를 한 번 더 호출합니다.
   `licbyl`(법령) / `admbyl`(행정규칙) / `ordinbyl`(자치법규).

**2. 못 받으면 "못 받았다" 를 컨텍스트에 명시**
   법제처는 별표 본문을 대개 HWP·PDF 첨부로만 줍니다. 본문을 못 받으면
   조문 자리에 이렇게 넣습니다.
```
(별표 4 의 본문은 법제처가 첨부파일(HWP/PDF)로만 제공하여 가져오지 못했습니다.
 이 별표가 정한 주기·기간·수치·기준은 확인할 수 없습니다.
 절대 추측하거나 기억으로 채우지 말고, '별표 4 원문을 확인해야 한다' 고 답하십시오.)
```
   비워두면 모델은 별표가 없다는 것조차 모릅니다. 있다는 사실과
   내용을 모른다는 사실을 함께 줘야 합니다.

**3. 그래도 지어내면 화면·PDF 에 경고**
   본문을 못 받은 별표를 답변이 근거로 들면 답변 맨 위에 경고를 답니다.

```
확인되지 않은 근거가 있습니다
답변이 「토양환경보전법 시행규칙」 별표 4 를 근거로 들었으나, 이 별표의 본문은
법제처가 첨부파일로만 제공하여 가져오지 못했습니다. 답변에 적힌 주기·기간·수치는
확인된 근거가 없으므로 그대로 사용하지 마시고 반드시 원본을 직접 확인하세요.
별표 4 원본 내려받기: https://www.law.go.kr/...
```

   PDF 와 복사에도 함께 나갑니다. PDF 는 결재·민원 안내에 쓰이는 기록이므로
   화면에만 띄우면 근거 없는 수치가 그대로 문서로 남습니다.

**4. 별표·서식 API 실측 결과 — 추측했던 것 세 가지가 틀렸습니다**

   실제로 호출해 보고 확정했습니다. 다음 사람이 같은 삽질을 하지 않도록 남깁니다.

| 항목 | 처음 넣었던 것 | 실제 | 결과 |
|---|---|---|---|
| 검색범위 | `section=lawNm` | **`search=2`** | 0건 → 40건 |
| 법령 지정 | `MST=법령일련번호` | **무시됨** | 엉뚱한 법령 반환 |
| 별표번호 | `lstrip("0")` | **6자리 분해** | `000400` → `400`(X) / `4`(O) |
| 제목 필드 | `별표제목` | **`별표명`** | 항상 빈 값 |
| 종류 필드 | `별표구분` | **`별표종류`** | 항상 빈 값 |
| 법령명 필드 | `법령명` | **`관련법령명`** | 항상 빈 값 |

   **`MST` 는 조용히 무시됩니다 — 이게 제일 위험했습니다.**
   `MST=281911`(토양환경보전법)을 줬더니 그 값을 버리고 `query` 로만 찾아
   **병역법·출입국관리법·지적측량 시행규칙의 별표 18건**을 돌려줬습니다.
   `resultCode 00 success` 라 실패로 보이지도 않습니다.
   MST 를 먼저 시도하고 "결과가 있으면 채택" 하도록 짜 두었으므로,
   그대로 뒀으면 **토양환경보전법 자리에 병역법 별표가 들어갔을 것**입니다.

   → MST 경로를 없앴습니다. `query` + `search=2` 만 씁니다.
   → 그리고 받은 결과를 **`관련법령명` 완전일치로 한 번 더 거릅니다.**
     `search=2` 는 부분일치라 "토양환경보전법" 으로 찾으면 시행령·시행규칙
     별표가 섞여 들어옵니다. 법령이 다르면 완전히 다른 규정입니다.

   **별표번호는 6자리 0패딩입니다.** 앞 4자리가 번호, 뒤 2자리가 가지번호.
```
000400 → 별표 4        001102 → 별표 11의2
000603 → 별표 6의3     002000 → 별표 20
```
   `lstrip("0")` 로 자르면 `000400` 이 `"400"` 이 되어 별표 4 를 영영 못 찾습니다.

   삭제된 별표(`삭제 <2005.6.30>`)는 걸러냅니다.

   검증 — 실제 응답 XML 로 파서를 돌렸습니다.
```
별표 4     별표   특정토양오염관리대상시설의 토양오염검사주기(제12조제2항 관련)  ← 찾던 것
별표 2     별표   특정토양오염관리대상시설(제1조의3관련)
별표 6의3  별표   검증수수료의 산정기준(제19조의7제2항 관련)
(삭제 별표는 제외됨)
```

**5. 본문 텍스트는 API 로 못 받습니다 — 긁지 않기로 했습니다**

   법제처 OPEN API 활용가이드 목록으로 **확정**했습니다. 추측이 아닙니다.

| 구분 | 분류 | 목록 조회 | 본문 조회 |
|---|---|---|---|
| 별표·서식 | 법령 | 법령 별표·서식 목록 조회 | **없음 (−)** |
| | 행정규칙 | 행정규칙 별표·서식 목록 조회 | **없음 (−)** |
| | 자치법규 | 자치법규 별표·서식 목록 조회 | **없음 (−)** |

   **별표는 목록 조회만 있고 본문 조회 API 자체가 없습니다.**
   제목·번호·파일링크까지가 API 가 주는 전부입니다.

   단 **행정규칙은 예외**입니다. 행정규칙 본문 조회(`target=admrul`)의
   출력 필드에 `별표`, `별표번호`, `별표가지번호`, `별표구분`, `별표제목`,
   **`별표내용`**, `별표서식파일링크` 가 들어 있습니다. 고시·훈령의 별표는
   본문에 딸려옵니다. 기존 `_extract_articles()` 의 `별표단위` 처리가
   이것을 받는 자리이고, 지금도 맞게 동작합니다.

   즉 못 받는 것은 **법령(법률·시행령·시행규칙)의 별표**뿐입니다.
   그리고 이번 문제(토양환경보전법 시행규칙 별표 4)가 정확히 그 경우입니다.

   목록 응답에 `별표내용` 필드가 없고, 상세 조회(`mode=detail`)도
   **iframe 껍데기**만 옵니다.

```html
<iframe src="http://www.law.go.kr/LSW/lsBylInfoP.do?bylSeq=18200569&lsiSeq=287443">
```

   실제 표는 저 안쪽 홈페이지 화면에 있습니다. 저것을 긁으면 API 가 아니라
   **웹 스크래핑**입니다. 법제처가 화면 구조만 바꿔도 조용히 깨지고,
   깨진 걸 모른 채 잘못된 표를 근거로 쓰게 됩니다. 그래서 긁지 않습니다.

   **대신 저 주소를 사람에게 줍니다.** `bylSeq`(별표일련번호)와
   `lsiSeq`(관련법령일련번호)가 목록 응답에 이미 있으므로 추가 호출 없이
   만들 수 있습니다. 누르면 브라우저에서 표가 바로 열립니다.

```
https://www.law.go.kr/LSW/lsBylInfoP.do?bylSeq=18200569&lsiSeq=287443
```

   HWP·PDF 첨부 링크보다 이쪽을 대표 링크로 씁니다. 첨부는 내려받아
   한글을 띄워야 열리지만, 이 주소는 클릭 한 번이면 됩니다.

   정리하면 **별표 수치는 담당자가 직접 확인합니다.** 도구는
   ① 어떤 별표를 봐야 하는지 제목으로 알려주고
   ② 한 번에 열리는 주소를 주고
   ③ AI 가 수치를 지어내면 경고로 막습니다.

### 수정 — 근거 조문에 엉뚱한 법령의 조문이 붙던 문제 (심각)
화면의 "근거 조문" 이 답변과 다른 법령을 보여주고 있었습니다.

| 답변이 인용한 것 | 화면에 붙은 조문 |
|---|---|
| 「토양환경보전법 시행규칙」 제12조제2항 (누출검사주기) | **법률** 제12조제2항 (신고수리 여부 통지) |
| 영 제8조제1항제1호 (석유류 저장시설) | **법률** 제8조제1항 (타인 토지에의 출입) |
| 영 제8조제1항제2호 (제한물질 등) | **법률** 제8조제1항 (타인 토지에의 출입) |

전부 법률로 떨어졌습니다. 원인이 둘이었습니다.

**1. `CITE_RE` 가 대괄호를 넘지 못함**
답변은 법령명을 `[토양환경보전법 시행규칙] 제12조제2항` 형태로 씁니다.
그런데 법령명 부분의 문자 집합에 `[` `]` `「` `」` 가 없어서 `]` 에서 끊겼습니다.
법령명이 빈 값이 되니 `_guess_law` 가 기본값(법률)으로 떨어뜨립니다.
→ 괄호류를 문자 집합에 넣고, `_norm()` 이 비교 전에 떼어냅니다.

**2. 법령 문언의 축약형을 모름**
법령은 자기들끼리 줄여 부릅니다. **시행령 → "영", 시행규칙 → "규칙", 상위 법률 → "법".**
별표 4 의 `영 제8조제1항제2호` 는 **시행령** 제8조인데 법률 제8조로 잡혔습니다.
`_SUFFIX` 에 `시행령`·`시행규칙` 만 있고 축약형이 없었습니다.
→ 축약형을 처리하되, **앞 글자가 한글이면 낱말의 일부로 봅니다.**
   "토양환경보전법" 의 "법" 을 축약형으로 오인하면 안 되기 때문입니다.

이것은 v1.20 대에 넣어둔 안전장치("법률 제3조와 시행령 제3조를 구분한다")가
정작 실제 답변 형식에서는 통째로 비켜 가고 있었다는 뜻입니다.

검증 — 8건 전부 통과.
```
[토양환경보전법 시행규칙] 제12조제2항  → 시행규칙   OK
영 제8조제1항제2호                     → 시행령     OK
법 제12조제1항                         → 법률       OK
「토양환경보전법 시행령」 제8조제1항     → 시행령     OK
규칙 제12조제2항                       → 시행규칙   OK
토양환경보전법 제12조제2항              → 법률       OK   ("법"을 축약형으로 오인 안 함)
제12조제1항 (법령명 없음)               → 법률       OK
```

### 수정 — 별표 원문 링크가 깨지던 문제
`renderArt()` 가 `파일링크` 앞에 `https://www.law.go.kr` 를 무조건 붙이고
있었습니다. 본문 조회로 딸려온 별표는 `/LSW/…` 상대경로라 맞았지만,
이번에 추가한 별표·서식 API 는 절대경로를 주므로
`https://www.law.go.krhttps://…` 가 되어 링크가 죽습니다.
→ 이미 `http(s)://` 로 시작하면 그대로 씁니다.

**6. `section=lawNm` — 처음 0건이 나온 이유**
   `target=licbyl` 은 맞았습니다. 그런데 첫 시도가 0건이었습니다.

```xml
<licBylSearch><target>licbyl</target>
  <키워드>토양환경보전법 시행규칙</키워드>
  <section>bylNm</section>          ← 여기
  <totalCnt>0</totalCnt>
  <resultCode>00</resultCode><resultMsg>success</resultMsg>
</licBylSearch>
```

   `resultCode 00 success` 인데 0건입니다. 호출은 성공했고 **검색 범위가
   달랐습니다.** `section` 을 안 주면 법제처 기본값이 `bylNm`(별표 이름)이라
   "토양환경보전법 시행규칙" 이라는 **이름의 별표**를 찾은 것입니다.
   법령명으로 찾으려면 `section=lawNm` 을 줘야 합니다.

   `section` 은 응답에 되돌아오는 **출력** 필드 이름이고, 요청에 넘기는
   **입력** 파라미터는 `search` 였습니다. 이름이 달라 헷갈리는 자리입니다.

   → `/api/raw` 가 `target`·`value`·`mode` 를 뺀 나머지 쿼리스트링을
     법제처로 그대로 넘깁니다. 앱을 고치지 않고 파라미터를 시험할 수 있습니다.
     예) `/api/raw?target=licbyl&value=토양환경보전법 시행규칙&search=2`
         `/api/raw?target=licbyl&value=18200569&mode=detail`

### 수정 — 되묻기 질문 3개가 통째로 사라지던 문제
로그에 `[clarify] 0개 질문 파싱 (원문 3줄)` 이 찍혔습니다.
모델은 멀쩡한 질문 3줄을 냈는데 파서가 전부 버렸습니다.

```
질문|누출검사주기가 달라진 사유는 무엇인가요?: 지하매설 저장시설 / 지상·옥내 저장시설 / 모름
```

기대 형식은 `질문|보기1|보기2|보기3` 입니다. 그런데 로컬 모델이
**형식 틀의 "질문" 이라는 낱말을 그대로 베껴** 앞에 붙이고, 정작 보기는
`:` 와 `/` 로 나눴습니다. `|` 로만 자르면 조각이 2개("질문" + 나머지 전부)라
v1.24.1 에서 넣은 "최소 3조각" 조건에 걸려 통째로 버려집니다.

→ `_split_clarify_line()` 을 새로 두었습니다.
   ① `질문`·`보기1` 처럼 **라벨만 있는 조각은 버리고**
   ② 조각이 모자라면 `질문: 보기 / 보기 / 모름` 형태를 한 번 더 풉니다.
   구분자는 `/` 만 씁니다. `·` 로 자르면 "지상·옥내 저장시설" 이 쪼개집니다.

검증(로그 원문 3줄 + 정상 형식 + 오작동 케이스 7건):

| 입력 | 결과 |
|---|---|
| `질문\|…?: 지하매설 저장시설 / 지상·옥내 저장시설 / 모름` | 질문 1 + 보기 3 ○ |
| `누출검사주기는?\|지하매설\|지상·옥내\|모름` | 질문 1 + 보기 3 ○ |
| `질문: 시설 종류는?\|보기1: 지하매설\|…` | 질문 1 + 보기 3 ○ |
| `질문\|보기1\|보기2\|보기3` (형식 틀 그대로) | 버림 ○ |
| `예\|아니오\|모름` (질문 없음) | 버림 ○ (v1.24.1 조건 유지) |

### 변경 — 법령 조회를 시행일 기준으로 (`target=eflaw`)
API 가이드를 읽어보니 법제처는 같은 법령을 두 기준으로 줍니다.

| target | 뜻 | 이 도구에 맞는가 |
|---|---|---|
| `law` | 현행법령(**공포일**) | 공포됐지만 시행 전인 조문이 섞일 수 있음 |
| `eflaw` | 현행법령(**시행일**) | **오늘 효력 있는 조문** ← 이쪽 |

지금까지 `law` 만 쓰고 있었습니다. 민원 안내는 오늘 효력 있는 조문으로
해야 하므로 기본값을 `eflaw` 로 바꿉니다.

같이 딸려오는 것들도 반영했습니다.
- `nw=3` — 현행만. 연혁본·시행예정본을 **서버가** 걸러줍니다.
  (기존에는 받아온 뒤 파이썬에서 `현행연혁구분` 으로 걸렀습니다)
- 되돌리기 — `.env` 에 `LAW_TARGET=law` 를 넣으면 즉시 원래대로.

**되돌리기 안전장치.** 문서상 `eflaw` 는 ID 만으로 현행 본문을 주지만,
실제로 조문을 못 받는 법령이 있을 수 있습니다. 그때 빈손으로 끝내면
답변 근거가 통째로 사라지므로, **조문이 안 오면 조용히 공포일 기준으로
다시 받습니다.** 어느 기준으로 받았는지는 `meta["조회기준"]` 에 남깁니다.

이 판정에 함정이 하나 있었습니다. `articles` 가 비었는지만 보면 안 됩니다.
`_extract_articles()` 는 구조를 못 읽으면 마지막에 **"(전문)" 통짜 항목
하나를 만들어 넣습니다.** 그래서 응답이 사실상 비어도 `articles` 는 1건이 되어
되돌리기가 동작하지 않았습니다. (테스트에서 실제로 걸렸습니다)
→ `_has_real_articles()` — 조문번호가 있는 항목이 하나라도 있거나
   본문 길이가 200자를 넘어야 "받았다" 로 봅니다.

### 추가 — 법령명을 몰라도 찾도록 (본문 검색 `search=2`)
이 도구의 가장 큰 약점은 첫 단계였습니다.

```
사용자: "주유소 기름탱크 누출검사 주기"
   ↓  AI 가 기억으로 법령명을 추측          ← 여기가 틀리면 그 뒤가 전부 틀어짐
법령명: 토양환경보전법, 위험물안전관리법
   ↓  이 이름으로만 검색
```

추측이 빗나가면 체계도가 통째로 비고, 이후 모든 단계가 엉뚱한 법령 위에서
돌아갑니다. 방사성폐기물관리법이 잡혔던 것(v1.24)도 여기서 시작했습니다.

가이드를 보니 `search=2` 로 **본문 검색**이 됩니다. 법령명이 아니라
조문 내용에서 찾는 것입니다. 법령명을 맞힐 필요가 없어집니다.

→ 두 군데에 복구 경로로 넣었습니다.
```
① 추측한 법령명으로 체계도를 못 찾으면
   → 용어("누출검사주기")로 본문 검색 → 진짜 법령명을 법제처에서 받아
     그 이름으로 체계도를 다시 탑니다
   → 처리 과정에 "법령명 복구(본문 검색)" 로 표시

② 그래도 아무것도 못 찾으면
   → 본문 검색 결과를 그대로 씁니다
   → 처리 과정에 "본문 검색으로 확보" 로 표시
```

**기존 흐름은 그대로 둡니다.** 법령명 검색이 성공하면 지금까지와 똑같이
동작하고, 실패했을 때만 새 경로가 뜁니다. 잘 되던 질문의 결과는 안 바뀝니다.

검증 — 실제 호출 없이 응답을 흉내 내어 7가지를 확인했습니다.
```
본문 검색      search=2 · nw=3 전달           OK
법령명 검색    search 파라미터 안 붙음         OK
빈 응답        eflaw → law 순서로 재조회       OK
              조문 확보 / 대체 사실 기록      OK
정상 응답      eflaw 한 번만 호출              OK
```

### 확인 — 별표 본문이 실제로 들어옵니다 (시행일 기준 전환의 뜻밖의 소득)
`target=eflaw` 로 바꾼 뒤 나온 PDF 를 열어보니 **별표 4 본문이 통째로**
조문 원문에 들어와 있었습니다.

```
[별표 4] 특정토양오염관리대상시설의 토양오염검사주기(제12조제2항 관련)
  1. 영 제8조제1항제1호 단서에 따라 토양오염방지시설을 설치한 경우…
     가. …5년ㆍ10년ㆍ15년이 되는 날 이후 90일 이내에 각각 1회
     나. …15년이 되는 날을 기준으로 매 2년이 되는 날 이후 90일 이내에 1회
     다. 동일부지 내 저장시설의 설치연도가 각각 다를 경우에는 …블록별로 적용한다.
  2. 영 제8조제1항제2호에 따라 저장시설 설치 후 10년이 지난 날부터 매 8년이
     되는 날 이후 90일 이내에 …1회 누출검사를 받아야 한다.
  3. 제1호에도 불구하고 …자연환경보전지역 …매년 토양오염도 검사를 받아야 한다.
```

**앞서 "법령 별표 본문은 API 로 못 받는다" 고 적은 것은 `target=law`
기준에서만 맞는 이야기였습니다.** 시행일 기준 본문 조회에는 별표가
`별표단위` 로 함께 옵니다. 기존 `_extract_articles()` 가 이미 그것을 받는
자리였고, 조회 기준만 바꾸니 그대로 동작했습니다.

그 결과 이번 답변의 수치는 **지어낸 것이 아니라 원문과 일치**합니다.

| 답변 | 별표 4 원문 | |
|---|---|---|
| 가목 5년·10년·15년 | 5년ㆍ10년ㆍ15년 이후 90일 이내 | 일치 |
| 나목 매 2년 | 15년 기준 매 2년 이후 90일 이내 | 일치 |
| 다목 블록별 적용 | 블록별로 적용한다 | 일치 |
| 제2호 10년 후 매 8년 | 10년이 지난 날부터 매 8년 | 일치 |

v1.26 초반에 "매 8년은 근거 없는 수치" 로 의심했던 것은 **틀린 판단**이었습니다.
그때는 별표가 컨텍스트에 없어 지어낸 것이 맞았고(1차·2차 답변의 수치가 서로
달랐던 것이 증거), 지금은 원문을 보고 답하고 있습니다.

별표 API(`licbyl`)와 경고 안전장치는 그대로 둡니다. 시행일 기준으로도 별표가
안 오는 법령이 있을 수 있고, 그때 조용히 지어내는 것을 막아야 하기 때문입니다.

### 수정 — 서식(별지)에 불필요한 경고가 뜨던 문제
```
확인되지 않은 근거가 있습니다
답변이 「토양환경보전법 시행규칙」별지 4, 「토양환경보전법 시행규칙」별지 5 를
근거로 들었으나, 이 별표의 본문은 …
```
**별지는 신고서·신청서 같은 서식입니다.** 주기·수치를 정하지 않으므로
본문이 없어도 답변에 지장이 없습니다. 그런데 경고가 뜨니, 정작 별표에
진짜 경고가 필요할 때 같이 무시하게 됩니다.

→ 별지·서식은 경고 대상에서 뺍니다. 링크는 그대로 남기고,
   컨텍스트에는 "제출용 서식입니다. 양식 자체는 주기·기준을 정하지 않습니다"
   라고만 적습니다. 경고는 **별표**에만 띄웁니다.

### 수정 — 문장 속 "영 제8조" 가 여전히 법률로 붙던 문제
v1.26 에서 축약형(영·규칙·법)을 처리했는데 절반만 동작했습니다.

```
영 제8조제1항제1호            → 시행령  OK
해당하면 영 제8조제1항제1호    → 법률    틀림
```

`_norm()` 이 공백을 지우기 때문입니다. `"해당하면 영"` → `"해당하면영"` 이 되어
`영` 앞이 한글이 되고, "낱말의 일부" 로 판정되어 축약형에서 제외됐습니다.
**문장 안에 있는 인용은 대부분 이 꼴**이라 실제로는 거의 다 틀리고 있었습니다.

→ 축약형 판정만 **공백을 지우지 않은 원본 접두사**로 봅니다.
   `"토양환경보전법"` 의 `법` 을 오인하지 않는 성질은 그대로 유지됩니다.

### 수정 — 법령명 없이 "(제12조제2항)" 만 쓴 인용이 법률로 붙던 문제
답변은 【근거】에서 이름을 밝히고, 【설명】에서는 문장 끝에 `(제12조제2항)` 만
붙입니다. 이때 기본값(법률)으로 떨어져, **시행규칙 제12조(검사주기)를 물었는데
법률 제12조(신고수리 여부 통지)가 근거로 붙었습니다.**

→ **마지막으로 이름이 명시된 법령**을 기준으로 삼습니다.
   축약형(`영`·`규칙`)은 기준을 바꾸지 않습니다. 그때그때 다른 것을 가리키므로
   기준으로 삼으면 뒤따르는 인용이 줄줄이 끌려갑니다.

검증 — 실제 PDF 답변 원문에서 뽑은 인용 9건 전부 정확.
```
「…시행규칙」 제15조의2      → 시행규칙   OK
「…시행규칙」 제12조제2항     → 시행규칙   OK
영 제8조제1항제1호           → 시행령     OK
…설치 시와 영 제8조제1항제2호 → 시행령     OK
…제1호에 해당하면 영 제8조…   → 시행령     OK   ← 이번에 고친 것
(제12조제2항)  법령명 없음    → 시행규칙   OK   ← 이번에 고친 것
```

### 수정 — 답변의 하이퍼링크가 전부 사라진 문제
답변 어디에도 링크가 없다는 보고. 원인은 **숫자 뒤 공백**이었습니다.

로컬 모델(Qwen)은 숫자와 뒷글자 사이를 띄웁니다.
```
영 제8 조제1 항제1 호   /   매 8 년마다   /   90 일 이내   /   2 만 리터
```

화면의 조문 링크 정규식은 `제\d+조` 를 찾는데 `제8 조` 는 걸리지 않습니다.
그래서 링크가 하나도 생기지 않았습니다.

**한쪽만 동작하고 있었던 것이 문제를 가렸습니다.** 서버의 인용 검증
정규식(`CITE_RE`)은 `제\s*(\d+)\s*조` 로 공백을 허용해서 근거 조문은
정상으로 뜹니다. 화면 링크만 조용히 죽어 있었습니다.

→ 두 곳을 고쳤습니다.
   ① `_clean_output()` 이 응답을 정리할 때 숫자 뒤 공백을 붙입니다.
      화면·PDF·복사·기록에 모두 반영됩니다. 읽기도 나아집니다.
   ② 화면의 `XREF_RE` 도 공백을 허용합니다. 이미 저장된 예전 기록과
      다른 모델의 출력에서도 링크가 살아납니다.

```
영 제8 조제1 항제1 호  →  영 제8조제1항제1호   (링크 걸림)
매 8 년마다 90 일 이내  →  매 8년마다 90일 이내
```

답변 프롬프트에도 **법령명을 「 」 로 감싸라**고 명시했습니다.
대괄호 `[토양환경보전법 시행규칙]` 로 쓰면 화면이 법령 링크를 못 붙입니다.

### 수정 — 되묻기에서 용량·이중벽 질문이 사라진 문제 (직전 수정의 부작용)
"저장시설 용량", "이중벽" 을 묻던 질문이 없어졌다는 보고.
**바로 앞에서 넣은 보기 필터가 너무 셌습니다.**

보기 자리에 질문이 들어오는 것을 막으려고 `인지` 로 끝나면 버렸는데,
```
탱크 구조는?|이중벽인지|단일벽인지|모름
저장시설 용량은?|2만리터 이상인지|2만리터 미만인지|모름
```
이런 **멀쩡한 보기까지 함께 버려졌습니다.** 보기가 2개 미만이 되어
줄 전체가 사라진 것입니다.

→ `_opt_is_question()` 으로 판정을 좁혔습니다.
   · 확실한 물음 어미(`인가요`·`입니까`·`습니까`·`나요`)로 끝나거나
   · **한 칸에 두 갈래를 담았을 때**(`석유류인지 유해화학물질인지`)
   이 둘만 질문으로 봅니다. `인지` 하나로는 버리지 않습니다.

검증 9건 — 용량·이중벽 질문 복구, 앞서 고친 세 버그도 그대로 통과.

### 수정 — 조회 기록에서 되묻기 조건이 빠지던 문제
같은 질문을 조건만 바꿔 네 번 조회했는데 기록이 전부 똑같아 보였습니다.
```
누출검사주기가 어케되었나요? 주유소이고 지하에 저장되어 있고…
오후 09:24 · 근거 14건
누출검사주기가 어케되었나요? 주유소이고 지하에 저장되어 있고…
오후 02:57 · 근거 14건      ← 무엇이 다른지 알 수 없음
```

v1.24 에서 질문 본문과 조건(`answered`)을 분리해 보내도록 바꿨는데,
기록에는 질문만 남기고 조건을 안 넣었습니다.

→ 기록에 조건을 함께 저장하고 목록에 표시합니다.
   기록을 다시 열 때 `LAST_RESULT.answered` 도 복원하므로,
   그 상태에서 PDF 를 저장해도 조건이 빠지지 않습니다.

### 수정 — 되묻기 형식 이탈 정리 (로컬 모델이 형식을 계속 어깁니다)
같은 뿌리에서 나온 문제를 세 번 고쳤습니다. 한자리에 모아 정리합니다.

프롬프트가 요구하는 형식은 `질문|보기1|보기2|보기3` 한 줄입니다.
그런데 로컬 모델(Qwen)이 이 형식을 지키지 않는 방식이 매번 달랐습니다.

| # | 모델이 낸 것 | 화면에서 벌어진 일 |
|---|---|---|
| 1 | `질문\|…?: 보기 / 보기 / 모름` | 조각이 2개라 **줄 전체가 버려짐** (0개 파싱) |
| 2 | `A인지 B인지\|C인지 D인지\|…\|모름` | **보기 자리에 다른 질문**이 들어감 |
| 3 | `질문\|보기 / 보기 / 모름\|모름` | **보기 셋이 한 칸에** 뭉쳐 고를 수가 없음 |

3번이 이번 것입니다.
```
지하 저장시설의 용량 범위
  ● 5,000 리터 미만 / 5,000 리터 이상 / 모름     ○ 모름
```
조각이 3개(질문+보기2)라 형식 검사는 통과합니다. 그런데 실제로 고를 수 있는
것은 뭉친 덩어리와 "모름" 둘뿐이라, 되묻기의 의미가 사라집니다.

→ `_expand_slash_options()` 를 앞단에 넣었습니다.
   보기 안에 `/` 가 있으면 갈라 펴고, 중복("모름" 이 두 번 나옴)을 없앱니다.
```
  ○ 5,000 리터 미만   ○ 5,000 리터 이상   ○ 모름
```

**구분자는 `/` 만 씁니다.** `·` 로 자르면 `지상·옥내 저장시설` 이 쪼개지고
`토양오염도검사·누출검사` 같은 법령 용어도 망가집니다. 이건 테스트로 고정했습니다.

정리하면 되묻기 한 줄은 이제 이 순서로 지나갑니다.
```
_split_clarify_line   형식 라벨("질문"·"보기1") 제거, 콜론 형식 복구
_expand_slash_options  "/" 로 뭉친 보기 펴기, 중복 제거
_repair_a_or_b         보기 자리의 질문 버리기, "A인지 B인지" 에서 보기 뽑기
_fix_yesno             예/아니오 질문에 부정 보기 채우기
```

검증 9건 — 위 세 버그 전부 복구, 정상 형식·예/아니오 보정 유지,
가운뎃점 보존 확인, 형식 틀만 베낀 줄은 여전히 버림.

> 근본 원인은 파서가 아니라 **로컬 모델이 형식을 못 지키는 것**입니다.
> 파서를 계속 넓히는 것으로 버티고 있으나, 이탈 형태가 또 나올 수 있습니다.
> 형식을 더 단순하게 바꾸거나(한 줄에 질문만, 보기는 다음 줄),
> 되묻기만 클라우드 모델로 돌리는 선택지를 고려할 시점입니다.

### 수정 — 보기 자리에 다른 질문이 들어가던 문제
```
지상·옥내 저장시설인지 지하 저장시설인지        1/6
  ○ 석유류인지 유해화학물질인지   ○ 15년 미만인지 15년 이상인지   ● 모름
```
질문 하나에 보기 세 개여야 하는데, **보기 자리에 다른 질문 두 개**가 들어갔습니다.
고를 수 있는 것이 "모름" 뿐이라 되묻기가 무의미해집니다.

모델이 질문 여러 개를 한 줄에 몰아넣은 것입니다.
```
지상·옥내 …인지 지하 …인지|석유류인지 유해화학물질인지|15년 미만인지 15년 이상인지|모름
   └── 질문 ──┘ └────── 원래 다른 질문이었던 것들 ──────┘
```

→ `_repair_a_or_b()` 를 넣었습니다.
   ① 보기 자리에 **질문 꼴**(…인지 / …인가요 / …습니까)이 오면 버립니다.
   ② 그래서 보기가 모자라면, 질문이 `A인지 B인지` 형태일 때
      **거기서 A·B 를 뽑아 보기로 세웁니다.**

버리기만 하면 되묻기가 통째로 사라지는데, 이렇게 하면 원래 물으려던 것이 살아납니다.
```
지상·옥내 저장시설인지 지하 저장시설인지
  ○ 지상·옥내 저장시설   ○ 지하 저장시설   ○ 모름
```

검증 5건 — 버그 재현 복구, 정상 형식 유지, 예/아니오 보정 유지,
v1.26 의 콜론 형식 유지, 형식 틀 그대로면 여전히 버림.

### 수정 — 드래그 복사에 하이퍼링크가 딸려가던 문제
답변을 마우스로 긁어 한글·워드에 붙이면 「법령명」이 파란 밑줄 링크로
따라갔습니다. 붙여넣는 곳에 따라 `[「토양환경보전법」](https://www.law.go.kr/%EB%B2%95...)`
처럼 주소가 통째로 글자로 나오기도 했습니다.

답변 원문에는 링크가 없습니다. 화면용으로 `linkify()` 가 붙인 것입니다.
"복사" 버튼은 `writeText` 라 원래 순수 텍스트였고, **마우스로 긁어
Ctrl+C 하는 경우**만 브라우저가 `text/html` 까지 클립보드에 넣고 있었습니다.

→ 답변·근거·조문 영역 안에서 복사할 때 `text/html` 을 링크 없는 형태로
   직접 덮어씁니다. 줄바꿈은 살립니다. 다른 영역의 복사는 건드리지 않습니다.

### 추가 — 항·호를 바꿔 말하거나 뒤에서 다른 번호로 바꾸지 않도록
답변 프롬프트에 두 줄 넣었습니다.
> 원문이 특정 항·호를 집어 예외나 다른 수치를 정하면 그 번호를 그대로 유지하라.
> 한 답변 안에서 같은 것을 서로 다른 항·호로 인용하지 마라.
> ("별표 4 제2호" 라고 했다가 "별표 4 제1호가목" 이라고 하면 하나는 지어낸 것)

### 검토 — 챗지피티 버전(`Old/files39~42_챗지피티버전`)
반영하지 않았습니다. 이유를 남깁니다.

| 변경 | 판단 |
|---|---|
| `main.py` 에 "방사성폐기물" 이름을 직접 넣어 제외 | 반영 안 함. 그 법령 하나만 막습니다. 이번 토양환경보전법 건에는 아무 효과가 없습니다 |
| "폐기물" 질문이면 폐기물관리법 체계를 강제로 추가 | 반영 안 함. 같은 이유. 분야마다 계속 늘려야 합니다 |
| `_BAD_CLARIFY_RE` 로 되묻기 필터 | 반영 안 함. `조문·법령·별표·기준` 이 들어간 질문을 전부 버립니다. 멀쩡한 질문까지 사라집니다 |
| `_fallback_clarify` 하드코딩 질문 | 반영 안 함. 사업장폐기물 전용입니다 |
| 프롬프트의 구체 예시 삭제 | **회귀**. "확인 필요를 보기로 쓰지 마라", "특정토양오염유발시설(X)" 같은 실제 사고 사례가 지워졌습니다 |
| `translate="no"` 삭제 | **회귀**. v1.25 자동번역 차단이 지워졌습니다 (1.24.1 기반이라 그렇습니다) |
| 항·호를 그대로 유지하라는 규칙 | **반영함**. 위 "추가" 항목. 다만 원본은 최우선 원칙 6번 중간에 끼워 넣어 6번 문장이 끊겨 있었습니다. 조문 인용 규칙으로 옮겨 담았습니다 |

### 수정 — 되묻기가 "모름"으로 다 답해도 같은 질문을 그대로 반복하던 문제
실사용 테스트 중 발견. 되묻기 4개 질문에 전부 "모름" 으로 답하고 다음 라운드로
넘어갔는데, 토씨 하나 안 틀리고 **똑같은 4개 질문이 3라운드 연속** 다시 나왔습니다.

```
빗물 배수구로 흘러가는 기름 섞인 물이 발생하는지 | 예 | 아니오 | 모름
기름 섞인 물이 발생한다면 그 원인이 지하매설 저장시설인지 지상·옥내 저장시설인지 | … | 모름
저장된 물질이 석유류인지 유해화학물질인지 | … | 모름
사업장 면적이 10제곱미터 이상인지 | … | 모름
```
(1·2·3라운드 — 서버 콘솔 로그로 완전 동일 확인)

`CLARIFY_PROMPT` 에는 "[이미 답변된 조건]에 나온 것은 절대 다시 묻지 마라" 는
지시가 이미 있었습니다. 문제는 로컬 모델(Qwen)이 이 지시를 그냥 무시한 것 —
프롬프트 지시만으로는 못 믿는다는 게 이번에도 확인됐습니다.

→ 프롬프트에 기대지 않고 코드에서 최종적으로 걸러냅니다.
   `_asked_questions(answered)` 가 `answered` 문자열("질문: 답변 / …")에서
   이미 물은 질문 텍스트만 뽑아 정규화된 집합으로 만들고,
   `ai_client.clarify()` 가 새로 낸 질문 중 이미 물은 것과 겹치면 버립니다.
   다 걸러지고 남는 게 없으면 되묻기를 끝내고 다음 단계(조문 선별)로 넘어갑니다.
   (`main.py` — `_norm_q()`, `_asked_questions()`, `_ask_after_search()`)

**실사용 재현 테스트로 확인함.** 같은 질문을 다시 넣고 전부 "모름"으로
답했더니, 1라운드는 새 질문 3개, 2라운드에서 모델이 또 (형식은 깨진 채로)
직전 라운드 질문을 되풀이했는데 이번엔 서버 콘솔에

```
[clarify] 이미 물은 질문 1개 반복 감지 → 되묻기 종료
```

가 찍히고 곧바로 조문 선별 단계로 넘어갔습니다. 3라운드째 그대로 반복되던
이전 증상이 재현되지 않았습니다.

### 변경 — MAXTOK_SELECT 200 → 400 → 800
같은 테스트에서 조문 선별(select) 단계 로그에 아래 경고가 떴습니다.
```
[local] ⚠ 출력이 상한(200)에서 잘렸습니다. .env 의 MAXTOK_* 를 올리거나 프롬프트를 줄이세요.
```
후보 조문이 많을 때 선택 번호 목록이 토큰 상한에서 잘려 뒤쪽 조문이 통째로
빠질 수 있습니다. 400으로 올려 재현 테스트했으나 **여전히 잘림**(103, 232~310 …
에서 끊김 — 후보가 최소 그 이상 있다는 뜻) 이 확인되어 800으로 다시 올렸습니다.
**기존 `.env` 파일은 자동으로 안 바뀝니다 — 직접 800으로 올려야 적용됩니다.**

400으로도 잘렸지만 다행히 앞쪽에 뽑힌 번호 안에 실제 정답 조문
(「물환경보전법」 제53조, 「물환경보전법 시행령」 제72조)이 포함돼 있어서
답변 자체는 정확했습니다. 다만 뒤쪽에 있었을 「물환경보전법 시행령」
별표 18(과태료 기준표)은 선택 목록에서 빠졌을 가능성이 있고, 실제로 답변도
"별표 18 금액은 제공된 조문에 없다" 고 정직하게 한계를 밝히는 것으로
끝났습니다 — 지어내지 않은 것은 맞지만, 후보가 더 잘리지 않았다면 별표까지
받아왔을 수도 있어 800으로 여유를 더 뒀습니다.

### 확인 — "법령명 복구(본문 검색)" 경로, 이번 테스트로는 발동 안 함
일부러 법령명을 헷갈리게 유도하는 질문("빗물 배수구로 기름 섞인 물…")으로
태워봤습니다. 로컬 모델이 실제로 "수질환경보전법"(정식 명칭 아님 — 옛 이름
"수질 및 수생태계 보전에 관한 법률"과 현재 이름 "물환경보전법"이 섞인 조어)을
냈는데도, 법제처 `lsStmd` 검색이 이 부정확한 이름을 느슨하게 매칭해
법령체계도(MST)를 찾아버렸습니다. 그래서 "완전 실패(0건)" 가 발생하지 않았고
본문검색 복구 로직도 타지 않았습니다.

즉 법제처 검색 API 자체가 이름 오차에 관대해서, 이 복구 경로는 실제로는
아주 드물게만 발동할 것으로 보입니다. 나쁜 소식은 아니지만 "정상 동작 확인"은
아직 못 했다는 뜻이라 남겨둡니다. 확인하려면 존재하지 않는 이름을 강제로
넣어보는 등 별도 테스트가 필요합니다.

### 확인 — 법제처 OPEN API 가이드 추가 조사 (법령정보 지식베이스, 위임법령 등 15건)
용어 연계 기능을 붙이기 전에, 법제처 가이드를 추가로 훑었습니다. 결론과 조사
근거를 남깁니다.

**당장 반영: 위임법령 조회(`lsDelegated`), 법률명 약칭 조회(`lsAbrv`)** — 아래
"추가" 항목 참고.

**반영 안 함(조사만) — `aiSearch`(지능형 법령검색)**
정확한 법령용어("특정토양오염관리대상시설")로는 20건 전부 정확한 법령만
나올 만큼 강력했지만, 사용자가 쓸 법한 애매한 문장("빗물 배수구 기름 섞인
물")으로는 정답(물환경보전법 제2조)이 9번째에 묻히고 나머지는 "빗물"·"기름"
낱말만 겹치는 무관한 조문(지방세법 시행령, 빗물이용시설 등)이 섞여 나왔습니다.
의미 이해가 아니라 키워드 검색에 가까워서, 지금 쓰는 본문검색(`search=2`)
복구 경로를 대체할 만큼 안정적이지 않다고 판단해 이번엔 넣지 않았습니다.

**반영 안 함(조사만) — `lsRlt`(관련법령 조회)**
법률-시행령-시행규칙 관계(법령간관계코드로 "기본법"·"하위법" 등 라벨까지
줌)를 깔끔하게 주길래 `lsStmd` 대신 쓸까 했으나, 실제 코드(`get_hierarchy()`)를
다시 보니 이미 `lsStmd` 하나로 법률·시행령·시행규칙·위임행정규칙까지 전부
가져오고 있어서 바꿀 이유가 없었습니다. `lsRlt`가 주는 정보는 이미 있는
정보의 부분집합입니다.

**검토는 했으나 이번엔 보류 — 조항호목 조회(`lawjosub`/`eflawjosub`)**
법 전체가 아니라 딱 필요한 조·항·호만 받는 API로, MAXTOK_SELECT 잘림 문제를
근본적으로 줄일 잠재력이 있습니다. 다만 지금 "법 전체를 통째로 가져와 목록에서
고르는" 구조를 "필요한 조문만 콕 집는" 구조로 바꿔야 해서, 이번 세션 내내
겨우 안정화한 조문 조회·별표 처리 로직을 다시 건드리게 됩니다. 한 번에 여러
개를 바꾸면 문제가 생겼을 때 원인 추적이 어려워지므로, 위임법령·약칭 두 개가
안정적으로 도는 걸 먼저 확인하고 별도로 다루기로 했습니다.

### 추가 — 위임법령 조회(`lsDelegated`) 연동
법률 조문이 실제로는 어느 시행령·시행규칙 조문에 위임됐는지, 법제처가
조문 단위로 직접 알려주는 데이터를 답변 컨텍스트에 힌트로 박아 넣습니다.

지금까지는 AI가 "법 제53조제1항제2호 → 시행령 제72조제3항" 같은 연결을
스스로 기억/추론해서 인용했습니다. 틀릴 수 있는 지점이었는데, 이제 법률
조문 본문 뒤에 이렇게 붙습니다.

```
[물환경보전법] 제53조(비점오염저감시설의 설치)
… (조문 본문) …
(※ 법제처 위임법령 데이터: 이 조문은 「물환경보전법 시행령」 제72조에
위임됩니다. 하위법령을 인용할 때는 이 조문번호를 그대로 쓰고, 다른 번호를
짐작해서 쓰지 마십시오.)
```

`law_client.get_delegated(law_id)` / `delegation_map(law_id)` 신규 함수로
가져오고, `main.py` 조문 수집 단계에서 **법률 조문에만**(시행령·시행규칙
자체에는 안 붙임 — 위임은 상위→하위 방향으로만 의미가 있음) 매칭되는 게
있으면 본문에 덧붙입니다. 처리 과정 패널에 "위임법령 힌트 추가 · N개"로
표시됩니다.

★ 이 API는 가이드 PDF에 필드 목록만 있고 실제 XML 예시가 없어서, 반복
레코드의 태그 이름을 실측하지 못했습니다. `조문번호`와 `위임법령조문번호`를
동시에 자식으로 가진 엘리먼트를 전부 레코드로 보는 방어적 방식으로 짰습니다.
**실사용 확인이 아직 안 됐습니다** — 처리 과정에 "위임법령 힌트 추가"가
안 뜨면 이 파싱이 안 맞았을 가능성이 있으니, `/api/raw?target=lsDelegated&value=<법령ID>&mode=detail`
로 원본을 한 번 봐야 합니다.

`verify_citations()`(인용 검증)에는 아직 연결하지 않았습니다. 이미 여러 번
패치된 복잡한 함수라, 이번엔 "AI가 애초에 틀린 번호를 안 쓰게" 하는
예방(컨텍스트 힌트)까지만 하고, "틀린 걸 사후에 잡아내는" 검증 연동은
힌트가 실제로 도는 걸 확인한 뒤 별도로 하기로 했습니다.

### 추가 — 법률명 약칭 조회(`lsAbrv`) 연동
법령명 복구 순서에 약칭 조회를 한 단계 끼워 넣었습니다.

```
1. 추측한 이름 그대로 체계도 조회
2. ★ 신규: 공식 약칭이면 정식명으로 바꿔 재조회      ← 이번 추가
3. (그래도 안 되면) 본문 검색으로 진짜 이름 복구      ← 기존 v1.26
```

`lsAbrv`는 법령명으로 검색하는 파라미터가 없어(전체를 페이지로만 받을 수
있음) `law_client.get_abbreviations()`가 첫 호출 때 최대 3,000건까지 페이지를
넘겨가며 받아 프로세스 수명 동안 캐시합니다. 이후 호출은 캐시에서 바로
찾으므로 매 질문마다 추가 API 호출이 생기지 않습니다.

★ "영"/"규칙" 같은 축약(예: "해당하면 영 제8조")은 이 API가 다루는 대상이
아닙니다. 그건 기존 `_guess_law()`의 별도 정규식 로직이 그대로 처리합니다.
`lsAbrv`는 법제처가 공식으로 등록한 법령 약칭(법률명 그 자체의 다른 이름)만
다룹니다.

---

## v1.25 — 2026-08-16

### 수정 — 브라우저 자동 번역이 화면과 답변을 뭉개던 문제
로컬 모델로 바꾼 뒤 되묻기 질문과 답변이 이상하게 나온다는 보고.

```
누출검사주기가 변경되었습니다대
분류 분류 분류 분류 / 3,000개의 리터러리
멸종 위기 / 스프레드류(석유류) / 토양환경보전법률 조정(시행규칙)
```

**모델 문제가 아니었습니다.** 화면의 하드코딩된 문구까지 바뀌어 있었습니다.

| 코드 원본 | 화면 표시 |
|---|---|
| 몇 가지만 **확인**할게요 | 몇 가지만 할게요 |
| 답이 **갈릴** 수 있는 / 더 **정확한** 조문 | 답이 갈 수 있는 / 더 나아가 조문 |
| 정확한 **수치를 적어주세요** | 그냥 존재하지 않으셔도 됩니다 |

이 문구는 HTML 에 고정되어 있어 AI 와 무관합니다.
**크롬 자동 번역이 한국어 페이지를 다시 한국어로 번역하며 뭉갠 것**입니다.
링크 주소는 `시행규칙` 으로 정확한데 표시 글자만 "법률 조정" 인 것이 결정적 증거입니다
(URL 은 번역 대상이 아니므로).

→ `<html translate="no">`, `<meta name="google" content="notranslate">`,
   `<body class="notranslate">` 를 넣어 자동 번역을 차단합니다.

**이미 번역이 걸린 탭에서는** 주소창의 번역 아이콘에서 "번역 안 함" 을 고르거나
새로고침해야 원래대로 돌아옵니다.

---

## v1.24.1 — 2026-08-16

### 수정 — 되묻기에서 "예" 가 질문으로 표시되던 문제 (v1.24 회귀)
```
질문
  예
    ○ 아니오   ○ 모름          ← "예" 가 질문 자리에
```

v1.24 에서 `_fix_yesno` 를 넣으면서 파싱 조건을 `>= 3` 에서 `>= 2` 로
완화한 것이 원인입니다. 모델이 질문 없이 `예|아니오|모름` 만 뱉은 줄까지
받아들여 첫 조각인 "예" 가 질문이 되었습니다.

→ 최소 3조각(질문 + 보기 2개) 조건을 되돌리고,
   **첫 조각이 보기 낱말**(예·아니오·모름·해당함 …)이면 그 줄을 버립니다.

검증: 버그 재현 케이스(`예|아니오|모름`) 0건 반환,
정상 줄과 섞여 있어도 정상 줄만 남습니다.

라운드당 질문 상한도 9 → 6 으로 맞췄습니다 (프롬프트와 불일치했음).

---

## v1.24 — 2026-08-14

### 수정 — 새 질문에 이전 질문의 법령이 그대로 남던 문제 (심각)
사업장폐기물 질문 뒤에 **완전히 다른 질문**(음식점 폐식용유)을 했는데
답변이 이렇게 나왔습니다.

```
제공된 조문 원문은 「방사성폐기물 관리법」, 같은 법 시행령·시행규칙
및 관련 규정으로만 구성되어 있습니다.
```

원인이 두 군데였습니다.

**1. 화면이 이전 상태를 안 지움**
"조문 찾기" 를 눌러도 `CURRENT_LAWS`·`LAST_RESULT`·되묻기 답변이 그대로 남았습니다.
→ `resetQueryState()` 추가. 질문 본문이 바뀌면 이전 법령·조문·조건·되묻기 답변을
   모두 비우고 라운드를 0으로 되돌립니다.

**2. 질문에 조건을 섞어 보내 캐시 키가 오염됨**
`question` 에 `\n(조건: …)` 을 붙여 전송하고 있었습니다.
→ 질문 본문과 조건(`answered`)을 분리해 전송합니다.

**3. 캐시가 질문 경계를 넘음**
→ 새 질문(`round=0`)이면 캐시를 쓰지 않고 **반드시 새로 검색**하며,
   기존 캐시 항목도 지웁니다. 되묻기 라운드 중(`round>0`)에만 재사용합니다.

### 수정 — 예/아니오 질문에 "아니오" 보기가 없던 문제
```
배출하는 폐기물이 방사성폐기물에 해당하나요?
  ○ 방사성폐기물   ○ 모름          ← "아니다" 를 고를 수 없음
```
→ `_fix_yesno()` 추가. "…에 해당하나요?" 형태인데 부정 보기가 없고
   실질 보기가 하나뿐이면 **예 / 아니오 / 모름** 으로 보정합니다.
   "지하매설 / 지상·옥내" 처럼 이미 대립하는 보기가 둘 이상이면 그대로 둡니다.
   프롬프트에도 좋은 예/나쁜 예를 넣었습니다.

### 추가 — 부정 조건을 검색에 반영
"아니오" 답변을 자연어로 프롬프트에 넣기만 하면 **검색 단계에는 반영되지 않습니다.**
"방사성폐기물이 아니다" 라고 답해도 방사성폐기물관리법이 계속 조회되던 이유입니다.

→ `_excluded_terms()` 로 부정 답변에서 주제어를 뽑아
   **검색 결과에서 해당 법령을 제외**합니다.
   (모든 결과가 걸리면 거르지 않아 빈 결과가 되지 않습니다)
   처리 과정에 "제외 조건 적용" 으로 표시됩니다.

```
방사성폐기물에 해당하나요?: 아니오
  제외 → 방사성폐기물 관리법, 같은 법 시행령
  유지 → 폐기물관리법, 폐기물관리법 시행규칙
```

### 추가 — 엉뚱한 법을 핵심으로 잡지 않도록
용어 변환 프롬프트에 명시했습니다.
> 질문이 일반적인 상황이면 일반법을 고르라.
> 질문에 명시되지 않은 특수·예외 분야 법을 1순위로 올리지 마라.
> "사업장에서 폐기물을 배출한다" → 폐기물관리법 (O) / 방사성폐기물 관리법 (X)

답변 프롬프트에도 조문이 질문과 다른 분야이면 **그 사실을 첫 줄에 밝히고
어떤 법령이 필요한지 알리도록** 추가했습니다.

---

## v1.23 — 2026-08-13

### 수정 — 출력 토큰 상한이 없어 모델이 무한정 생성하던 문제 (성능)
Ollama server.log 실측입니다.

| 단계 | 입력 | **출력** | 시간 |
|---|---|---|---|
| 용어 변환 | 439 | **4,243** | 55초 |
| 되묻기 | 2,179 | **7,611** | 100초 |
| 되묻기 | 2,179 | **9,551** | 127초 |
| (관측) 답변 | 9,006 | **20,443** | 160초 후 HTTP 500 |

용어 변환은 두 줄만 뱉으면 되는데 4,243 토큰을 생성했습니다.
생성 속도(초당 77토큰)는 정상이므로 **순전히 출력량 문제**입니다.
Ollama 는 `num_predict` 를 주지 않으면 무제한 생성합니다.

→ 단계별로 상한을 걸었습니다. 필요한 분량이 다르므로 따로 잡습니다.

```
MAXTOK_TERMS=300      # 두 줄
MAXTOK_SELECT=200     # 숫자 나열
MAXTOK_CLARIFY=800    # 질문 몇 줄
MAXTOK_ANSWER=4000    # 근거 + 설명
```

로컬은 `options.num_predict`, OpenAI 호환은 `max_tokens`,
Gemini 는 `generationConfig.maxOutputTokens` 로 전달합니다.

**예상 효과** — 용어 변환 55초 → 4초 안팎. 되묻기·답변도 비례해 줄어듭니다.

### 추가 — 잘림 감지와 오류 로깅
- 출력이 상한에서 잘리면(`done_reason=length`) 서버 콘솔에 경고를 남깁니다.
  잘리면 형식이 깨져 파싱이 실패하므로, 그때 `MAXTOK_*` 를 올리면 됩니다.
- 로컬 LLM 이 5xx 를 반환하면 요청 모델·상한과 응답 본문을 콘솔에 기록합니다.
  (HTTP 500 의 실제 원인을 확인할 수 있게)

### 추가 — 답변 분량 억제
프롬프트에 명시했습니다.
- 【근거】는 한 줄에 하나. 조문 원문을 그대로 옮기지 말고 한 줄 요약.
- 【설명】은 전체 15줄 안쪽. 같은 말을 반복하지 말 것.
- 조문 원문은 화면 오른쪽에 이미 있으므로 답변에 길게 재인용하지 말 것.

### 유지
법령 검색 · 조문 선별 · 별표 자동 포함 · 인용 검증 로직은 변경하지 않았습니다.
(272개 중 7개 선별, 별표 자동 포함, 인용 검증 4/4 정상 동작 확인)

---

## v1.22 — 2026-08-13

### 추가 — 인용된 별표를 자동으로 끌어옴 (가장 중요)
시행규칙 제12조제2항은 **"누출검사주기는 별표 4와 같다"** 고만 정합니다.
실제 주기(8년·90일 등)는 별표 4 에 있는데, 별표를 안 가져오면
AI 가 그 숫자를 제12조제2항이 정한 것처럼 쓰거나 지어냅니다.

→ 선별된 조문 본문에서 `별표 N` / `별지 N` 인용을 찾아
   **해당 별표를 조문 목록에 자동으로 추가**합니다.
   인용되지 않은 별표는 넣지 않습니다.

실측: 제12조제2항만 선별된 상태에서 별표 4 만 자동 포함,
별표 3·5 는 제외. 처리 과정에 "별표 자동 포함" 으로 표시됩니다.

조문 선별 프롬프트에도 "기준·주기 같은 수치는 별표에 있으니
적극적으로 고르라" 를 추가했습니다.

### 수정 — 답변에 "중요" 라는 말이 법령 용어처럼 섞이던 문제
`"그 후에는 중요 저장시설 설치 후 10년이 지난 날부터…"`

법령에 "중요 저장시설" 이라는 개념은 없습니다.
프롬프트의 강조 마커 `!!중요!!` 를 모델이 **문자 그대로 출력**한 것이
렌더링되어 문장에 끼어든 것입니다.

→ `!!중요!!` 마커를 프롬프트에서 제거. **굵게**·__밑줄__ 만 남깁니다.
   "굵게·밑줄 외의 강조 기호를 만들어 쓰지 마라" 를 명시했습니다.

### 추가 — 위임·인용 처리 규칙
답변 프롬프트에 다음을 명시했습니다.
1. 법률·시행령·시행규칙 조문을 섞어 하나의 규정처럼 설명하지 말 것
2. "별표 N 과 같다", "대통령령으로 정한다" 처럼 **넘기는 조문**은
   넘긴다는 사실만 말하고, 구체적 숫자를 그 조문이 정한 것처럼 쓰지 말 것
3. 넘겨받은 별표가 원문에 없으면 **숫자를 지어내지 말고**
   "관련 별표 원문이 확인되지 않아 확정할 수 없다" 고 쓸 것
4. **최초 검사와 정기검사를 구분**할 것
5. **대상 여부와 주기를 구분**할 것
   (예: 「위험물안전관리법 시행령」 제17조 정기검사 대상시설은 제외)

---

## v1.21 — 2026-08-13

### 수정 — 질문과 무관한 위임행정규칙이 딸려오던 문제
토양오염 질문에 **「금강수계 수변구역 변경」**, **「소방청 소관 법률에 따른
행정처분 및 과태료 부과 세부 기준」** 이 함께 조회됨.

원인: 법령체계도가 반환한 위임행정규칙을 **앞에서부터 4개** 그냥 가져왔음.
체계도에는 그 법령에 위임된 모든 고시가 들어 있어 무관한 것이 섞임.

→ 검색어를 2글자 조각으로 쪼개 규칙 이름과 겹치는 정도로 점수를 매기고,
   **0점(전혀 안 겹침)은 제외**, 나머지를 점수 순으로 최대 4개.

실측
```
11점  특정토양오염관리대상시설 관리지침      ← 선택
 2점  토양관련전문기관 지정·관리 업무처리지침  ← 선택
 0점  금강수계 수변구역 변경                ← 제외
 0점  소방청 소관 법률에 따른 …              ← 제외
```

### 수정 — `<img>` 태그와 메타데이터가 본문에 섞이던 문제
법제처가 표·그림을 이미지로 넣은 자리에 `<img id="163356319"></img>` 가
그대로 노출되고, 담당자명·전화번호·첨부파일 URL 까지 본문에 들어갔음.

→ `_clean_body()` 추가
- `<img …>` → `[그림·표 생략]`
- `flDownload.do` 다운로드 URL 제거
- 구조를 못 읽은 경우의 폴백을 `itertext()` 전체 긁기에서
  **내용 태그만 선별**(`조문내용`·`제개정이유내용`·`부칙내용`)로 변경.
  기본정보(발령번호·소관부처·담당자·전화번호)가 본문에 섞이지 않습니다.

---

## v1.20 — 2026-08-13

### 수정 — 사고 과정(Thinking)이 실제로는 꺼지지 않고 있었음
v1.18 에서 프롬프트 끝에 `/no_think` 를 붙이는 방식으로 껐다고 했으나,
**Ollama 의 server.log 확인 결과 사고 과정이 그대로 생성되고 있었습니다.**
응답 본문에 `<think>` 가 없었던 것은 Ollama 가 이를 별도 필드로 분리했기 때문이며,
생성 시간은 그대로 소모되고 있었습니다. (용어 변환 58초의 실제 원인)

→ **Ollama 네이티브 API 로 전환.** `LOCAL_NO_THINK=1` 이면
   `/api/chat` 에 `think: false` 를 보냅니다.
   모델 지시문이 아니라 **API 파라미터**이므로 확실히 적용됩니다.

```
LOCAL_NO_THINK=1  →  POST /api/chat        {"think": false, …}
LOCAL_NO_THINK=0  →  POST /v1/chat/completions   (OpenAI 호환)
```

`LOCAL_BASE_URL` 의 `/v1` 을 떼어 네이티브 주소를 자동으로 만듭니다.
Ollama 가 아닌 서버(LM Studio·llama.cpp)를 쓰면 `LOCAL_NO_THINK=0` 으로 두세요.
서버가 `think` 를 받지 않으면 그렇게 하라는 안내가 나옵니다.

`LLM_DEBUG=1` 로그에 어느 경로를 썼는지와 `thinking` 필드 내용이 함께 표시됩니다.

---

## v1.19 — 2026-08-13

### 수정 — 되묻기 라운드마다 검색을 처음부터 다시 하던 문제 (성능)
로그에서 확인된 낭비입니다.

```
1라운드  용어 변환 58초 → 법제처 조회 15초 → 되묻기 104초
2라운드  용어 변환 59초 → 법제처 조회 14초 → 되묻기 118초   ← 앞 두 단계 반복
3라운드  용어 변환 …   → 법제처 조회 …    → …              ← 또 반복
```

법제처 요청 URL 이 라운드마다 **완전히 동일**했습니다.
v1.7 에서 되묻기를 조문 수집 뒤로 옮기면서(용어를 지어내는 문제 때문에)
`/api/ask` 가 매 라운드 처음부터 재실행되는 구조가 되었습니다.

→ 질문·검색대상이 같으면 **검색 결과를 재사용**합니다 (10분 캐시).
   되묻기 답변은 검색 결과를 바꾸지 않으므로 캐시 키에서 제외했습니다.

실측 (3라운드 기준)
| | 이전 | 이후 |
|---|---|---|
| 용어 변환 | 3회 | **1회** |
| 법제처 조회 | 6회 | **2회** |

Gemini 에서도 동일하게 낭비되고 있었습니다(호출이 빨라 눈에 띄지 않았을 뿐).
법제처 API 부하도 함께 줄어듭니다.
처리 과정에 "이전 검색 재사용" 으로 표시됩니다.

### 수정 — 되묻기 보기에 라벨이 그대로 붙던 문제
로컬 모델이 출력 형식의 라벨을 답에 포함시켰습니다.

```
질문: 누출검사를 받으시는 시설의 종류는? | 보기1: 지하매설 저장시설 | 보기2: 지상·옥내 | 보기3: 모름
```

파서가 `질문` 을 질문으로, 나머지를 보기로 잘라 화면이 어긋났습니다.
→ `질문:` `보기1:` `선택지2:` `option1:` 같은 라벨을 제거하도록 수정.

### 확인 — `/no_think` 는 정상 적용됨
`LLM_DEBUG=1` 로그에 `<think>` 블록이 없었습니다.
첫 호출이 58초 걸린 것은 모델 6.6GB 를 VRAM 에 올리는 시간이 포함된 것입니다.

---

## v1.18 — 2026-08-11

### 수정 — 로컬 모델이 되묻기에서 몇 분씩 멈추던 문제
두 가지가 겹쳤습니다.

**1. Qwen 의 사고 과정(Thinking)**
Qwen3 계열은 답하기 전에 영어로 긴 추론을 생성합니다.
이 프로그램은 형식 준수가 중요하지 추론이 필요한 작업이 아닌데,
짧은 답 하나에도 수십 초~수 분이 걸렸습니다.
→ 프롬프트 끝에 `/no_think` 를 붙여 끕니다 (`LOCAL_NO_THINK=1`, 기본 켜짐).
   지원하지 않는 모델은 무시하므로 붙여도 해가 없습니다.

**2. 되묻기에 조문 목록을 통째로 넣던 것**
14개 법령을 찾으면 조문 제목이 수백 줄이 되는데, 그것을 전부 넣고 있었습니다.
갈래 판단에는 그만큼 필요하지 않습니다.
→ 앞 60줄만 넘기고 나머지는 "… (외 N개)" 로 표시 (`CLARIFY_CATALOG_LINES`).

### 수정 — 되묻기 보기에 "할 일" 이 나오던 문제
로컬 모델이 이런 보기를 만들었습니다.
```
시설의 종류는?   ○특정토양오염관리대상시설  ○제3조에 따른 제외 여부 확인 필요  ○모름
검사 면제 대상?  ○시행규칙 제8조의2 면제 등  ○면제 신청 여부 확인 필요  ○모름
```
"확인 필요" 는 답이 아니라 할 일이고, 조문 번호는 질문자가 모르는 것입니다.

→ 프롬프트에 명시:
- 보기는 **질문자가 현장에서 아는 사실**이어야 한다 (좋은 예/나쁜 예 병기)
- "확인 필요", "검토 요망" 같은 할 일을 보기로 만들지 마라
- 보기에 조문 번호를 쓰지 마라
- 한 번에 최대 6개 (기존 9개 → 축소)

---

## v1.17 — 2026-08-11

### 수정 — HTTP 524 (Cloudflare 타임아웃)
답변 생성이 길어지면 `524` 로 실패. Cloudflare 무료 플랜은 **원본 서버가
100초 동안 아무 데이터도 보내지 않으면** 연결을 끊습니다.
로컬 모델은 그보다 오래 걸리므로 사실상 매번 실패합니다.

→ `/api/ask` 를 **NDJSON 스트리밍**으로 변경.
   처리를 백그라운드로 돌리면서 단계가 끝날 때마다 한 줄씩 내보내고,
   진전이 없으면 5초마다 신호를 보내 연결을 유지합니다.
   기존 처리 로직은 그대로 두고 바깥만 감쌌습니다.

**부수 효과** — 화면의 "처리 과정" 이 **실시간으로 채워집니다.**
기존에는 20~30초를 스피너만 보고 기다렸으나, 이제 어느 단계인지 보입니다.

### 변경 — `.env` 를 옵션 1 / 옵션 2 구조로 재정리
`LLM_BACKEND` **한 줄**로 전체를 전환합니다.

```
LLM_BACKEND=gemini    # 옵션 1 — 빠르고 정확. 데이터가 구글로 전송됨
LLM_BACKEND=local     # 옵션 2 — 데이터가 PC 밖으로 안 나감. 느림
```

단계별 혼용(`LLM_BACKEND_ANSWER=local` 등)은 그대로 가능하며 위 값보다 우선합니다.
설정 파일을 용도별 블록으로 재배치하고, 각 항목에 선택 기준을 적었습니다.

**로컬로 돌릴 때 권장값**
| 항목 | Gemini | 로컬 |
|---|---|---|
| `CONTEXT_LIMIT` | 60000 | 20000~30000 |
| `CLARIFY_ROUNDS` | 6 | 3~4 |
| `LOCAL_TIMEOUT` | — | 300 |

---

## v1.16 — 2026-08-11

### 수정 — 다른 지자체 조례가 섞여 나오던 문제
"성남시 조례" 자리에 인천광역시·수원시 등 **엉뚱한 지자체 조례**가 표시됨.

원인: 검색어에 "성남시" 를 붙여도 법제처가 부분 일치로 다른 지자체 결과까지
돌려주는데, 받아온 것을 **그대로 담고 있었음.** 지자체 필터가 없었음.

→ 검색 결과의 `지자체기관명` 을 확인해 우리 지자체가 아니면 버립니다.
→ 본문 조회 후에도 한 번 더 확인 (v0.7 의 오조회 방지 장치에 지자체 검사 추가).
   걸리면 "○○ 조례여서 제외했습니다" 로 이유를 표시.

실측: 4건(성남시 2 · 인천 1 · 수원 1) 중 성남시 2건만 통과.

### 추가 — `LOCAL_GOV` 설정
지자체 이름을 `.env` 로 분리했습니다. 코드에 "성남시" 가 박혀 있던 것을 제거.

```
LOCAL_GOV=성남시
```

다른 지자체에서 쓰려면 이 줄만 바꾸면 됩니다. 비우면 필터가 꺼집니다.

---

## v1.15 — 2026-08-11

### 수정 — 조약·협정이 깨진 링크로 걸리던 문제
「한ㆍ일 어업협정」, 「한ㆍ중 어업협정」 을 클릭하면 법제처에서 "없다" 고 나옴.
`「」` 안의 **모든 문자열**을 `law.go.kr/법령/…` 로 연결하고 있었는데,
조약·협정은 법령 검색 경로에 없어 404 가 남.

→ `isLawName()` 추가. 법령으로 보일 때만 링크합니다.
- **링크함** — 법·법률·령·시행령·규칙·시행규칙·조례·훈령·예규·고시·지침·규정 으로 끝나는 것
- **링크 안 함** — 협정·조약·협약·헌장·의정서·각서·선언 으로 끝나는 것

검증 9종 통과 (한ㆍ일 어업협정·기후변화협약·남북기본합의서는 제외,
토양환경보전법·위험물안전관리법 시행령·성남시 ○○ 조례·관리지침은 링크).

### 제거 — 로컬 실패 시 Gemini 자동 재시도
v1.14 에서 넣은 `LOCAL_FALLBACK` 을 제거했습니다.
로컬 모델의 실제 성능을 측정하는 것이 목적인데,
실패를 자동으로 덮어버리면 어느 단계가 안 되는지 알 수 없습니다.
실패하면 실패한 대로 보이고, `.env` 에서 해당 단계를 Gemini 로 되돌리면 됩니다.

---

## v1.14 — 2026-08-11

### 수정 — 로컬 모델이 프롬프트 양식을 그대로 베끼던 문제 (심각)
Qwen3.5-9B 로 용어 변환을 돌리자 검색어가 이렇게 나왔음.

```
법령명: ["용어1","용어2 (Wait","LawA","LawB","Law1","Law2", …]
용어  : ["Term3","Term4","Term5", …]
```

프롬프트의 **양식 자리표시자**(`용어1`, `LawA`, `Term3`)를 실제 답으로 베끼고,
사고 과정 조각("Wait", "But usually", "related terms")까지 섞여 나옴.
그 쓰레기로 검색해 **총허용어획량 시행계획**, **성평등가족부 운영규정** 이 조회됨.

v1.13 에서 파싱을 관대하게 만든 것이 오히려 쓰레기까지 주워 담는 결과가 됨.

**세 겹으로 수정**
1. **프롬프트에서 자리표시자 제거** — 추상 양식(`용어1, 용어2`) 대신
   실제 예시 3개만 제시. 베낄 자리표시자 자체를 없앰.
   "영어를 쓰지 마라", "백틱·따옴표·번호를 붙이지 마라" 명시.
2. **쓰레기 필터** (`_is_junk`) — 자리표시자 패턴, 숫자만, 영어만,
   한글이 없는 것, 사고 과정 조각(wait/but/usually/however)을 걸러냄.
   `용어2 (Wait` 같은 꼬리도 절단.
   법령명 자리에 법령처럼 생기지 않은 것이 오면 용어로 강등.
3. **자동 폴백** (`LOCAL_FALLBACK=1`) — 로컬 결과에 법령명이 하나도 없으면
   Gemini 로 한 번 더 시도. 작은 모델이 실패해도 서비스가 멈추지 않음.

실측 검증: 위 쓰레기 응답에서 `토양환경보전법, 위험물안전관리법` /
`특정토양오염관리대상시설, 토양정밀조사, 누출검사` 만 정확히 추출.

### 수정 — 인쇄 시 URL 이 본문을 뒤덮던 문제
v1.10 의 인쇄 CSS 에 `a.lawref::after{ content:" (" attr(href) ")" }` 가 있어,
브라우저 인쇄로 뽑으면 문장 사이에 긴 URL 이 그대로 박혔음.
종이로 봐도 출처를 알 수 있게 한 것이었으나 가독성을 크게 해침.
→ 규칙 제거. 링크는 이름만 남기고 점선 밑줄로 표시.

**출처가 필요하면 "PDF 저장" 버튼을 쓰세요.** 서버가 문서를 직접 만들어
URL·머리말·페이지번호가 섞이지 않습니다.

---

## v1.13 — 2026-08-11

### 수정 — 로컬 모델에서 "검색어를 만들지 못했습니다"
`LLM_BACKEND_TERMS=local` 로 바꾸자 용어 변환이 실패.
파서가 `법령명: …` / `용어: …` 형식을 **정확히** 맞춰야만 동작했는데,
작은 모델은 형식을 지키라고 해도 마크다운·코드펜스·사고 과정을 섞어 냄.

### 추가 — 응답 정리 (`_clean_output`)
Gemini·로컬 양쪽 응답에 공통 적용.
- `<think>…</think>` 및 닫히지 않은 `<think>` 이후 전부 제거
- 코드펜스(```) 제거
- 마크다운 강조(`**법령명:**`) 제거, 머리글머리표 제거
- 전각 문자(`：` `，`) → 반각

Ollama 가 본문 대신 `reasoning_content` 필드로만 주는 경우도 대응.

### 추가 — 파싱을 형식 흔들림에 강하게
**용어 변환** — 라벨 변형(`법령`/`법률명`/`키워드`) 허용,
라벨이 아예 없으면 줄 단위로 훑어 **법·령·규칙으로 끝나면 법령명**, 아니면 용어로 분류.
번호 목록(`1. …`)·따옴표·괄호 제거.
검증한 형식 7종 모두 통과 (정상/마크다운/코드펜스+think/전각/라벨없음/번호목록/라벨변형).

**되묻기** — `OK.` `OK 입니다` 처럼 덧붙여도 종료로 인식.
번호·마크다운이 섞인 줄도 파싱.

**조문 선별** — 설명이 섞여도 번호만 추출.
단, `제8조제1항` 같은 조문 번호는 **먼저 제거**해 선택 번호로 오인하지 않도록 함.

### 추가 — `LLM_DEBUG=1`
프롬프트와 응답 원문, 파싱 결과를 서버 콘솔에 출력.
로컬 모델이 무엇을 뱉는지 확인할 때 사용.
검색어 변환 실패 시 화면 안내에도 이 설정을 알려줌.

---

## v1.12 — 2026-08-09

### 추가 — 로컬 LLM 백엔드 (Ollama 등)
`.env` 에서 **단계별로** Gemini 와 로컬 모델을 골라 쓸 수 있습니다.

```
LLM_BACKEND=gemini            # 기본값
LLM_BACKEND_ANSWER=local      # 답변 생성만 로컬로
LOCAL_BASE_URL=http://localhost:11434/v1
LOCAL_MODEL=qwen3.5:9b-q4_K_M
```

- OpenAI 호환 `/chat/completions` 규격을 씁니다. Ollama·LM Studio·llama.cpp 모두 동일.
- `<think>…</think>` 로 감싼 사고 과정은 자동 제거 (형식 파싱이 깨지지 않도록).
- 로컬은 생성이 느리므로 타임아웃 기본 180초 (`LOCAL_TIMEOUT`).
- 연결 실패·모델명 오류를 구분해 안내 (`ollama list` 로 이름 확인 유도).
- 헤더에 `로컬 2/4` 형태로 몇 단계가 로컬인지 표시.

**단계별 난이도** — 쉬운 것부터 옮겨가며 비교하기를 권장.

| 단계 | 난이도 | 비고 |
|---|---|---|
| terms | 낮음 | 일상어 → 법령용어 |
| select | 중간 | 조문 제목 보고 고르기 |
| clarify | 높음 | 어디서 조문이 갈리는지 판단 |
| answer | 높음 | 조·항·호 정확도가 관건 |

**비교 지표**는 화면의 "인용 검증 n/m건" 을 그대로 쓰면 됩니다.
법률 제8조와 시행령 제8조를 구분하는지가 실사용 가능 여부를 가릅니다.

---

## v1.11 — 2026-08-09

### 변경 — PDF 를 서버에서 직접 생성
v1.10 의 PDF 버튼은 브라우저 인쇄 대화상자를 여는 방식이었음.
그런데 인쇄 대화상자의 **기본 대상이 "Microsoft Print to PDF"** 이고,
이것으로 저장하면 화면이 **이미지로 렌더링되어 텍스트 복사·검색이 안 됨.**
(제출된 PDF 메타데이터에 `/Producer (Microsoft: Print To PDF)` 로 확인)

크롬 내장 "PDF로 저장" 을 고르면 해결되지만,
**사용자에게 매번 대상을 바꾸라고 요구하는 것은 잘못된 설계**이므로
서버에서 직접 PDF 를 만들어 내려받는 방식으로 변경.

- 새 모듈 `pdf_maker.py` (reportlab)
- 새 엔드포인트 `POST /api/pdf`
- 버튼을 누르면 파일이 바로 내려받아짐. 인쇄 대화상자가 뜨지 않음.
- 파일명 `법령조회_20260809_1530.pdf` (RFC 5987 로 한글 인코딩)

**문서 구성**: 질문 → 확인된 조건 → 근거 조문 → 답변 → (쪽 나눔) → 조문 원문 → 고지
답변의 `**굵게**` `__밑줄__` `!!중요!!` 표시가 PDF 서식으로 변환됨.
조문은 항·호·목 단위로 줄바꿈. 카드가 페이지 중간에서 잘리지 않음.

### 한글 폰트
| 환경 | 폰트 | 준비 |
|---|---|---|
| 윈도우 | 맑은 고딕 | 기본 설치됨 |
| 라즈베리파이·리눅스 | 나눔고딕 | `sudo apt install fonts-nanum` |

**주의**: `NotoSansCJK-*.ttc` 는 CFF(포스트스크립트) 윤곽선이라
reportlab 이 읽지 못합니다. 후보에서 제외했으며, 폰트를 찾지 못하면
설치 방법을 안내하는 오류 메시지를 반환합니다.

### 추가 패키지
```
pip install reportlab
```

---

## v1.10 — 2026-08-09

### 추가 — PDF 저장 버튼
답변 영역 우측에 "PDF 저장" 버튼. 누르면 브라우저 인쇄 대화상자가 열리고,
대상을 "PDF로 저장" 으로 고르면 됩니다.

**텍스트 복사가 안 되던 문제** — 브라우저 인쇄는 원래 텍스트를 그대로 남깁니다.
복사가 안 됐다면 화면 캡처였거나 인쇄 옵션의 "그림으로 인쇄" 가 켜져 있던 경우입니다.
다만 인쇄용 스타일이 없어 화면 그대로 출력되던 문제는 실재했으므로 정비했습니다.

**인쇄 시 적용되는 것** (`@media print`)
- 햄스터·버튼·입력창·되묻기·조회이력 제외
- **접힌 조문을 모두 펼쳐서** 출력 (화면에서 접혀 있어도 PDF 에는 전부 나옴)
- 질문을 문서 제목처럼 상단에 표시
- 카드가 페이지 중간에서 잘리지 않도록 `break-inside:avoid`
- 법령 링크는 주소를 각주처럼 병기 (종이로 봐도 출처 확인 가능)
- A4 여백 설정, 흑백 인쇄에 맞춘 색 조정
- 파일명이 `법령조회_20260809` 형태로 제안됨

### 수정
`secure=True` 주석 해제. HTTPS 전용 쿠키가 적용됩니다.
로컬(`http://127.0.0.1:8000`) 접속에서는 로그인이 되지 않으니
`https://law.gwiyomi-lab.com` 으로 사용하세요.

---

## v1.9 — 2026-08-09

### 추가 — 3단비교 버튼
법제처의 3단비교(법률·시행령·시행규칙을 나란히 보기) 화면으로 바로 연결.

**직접 구현하지 않고 링크로 처리한 이유**
- 법제처는 **위임조문 매칭이 이미 되어 있음.** 흉내 내면 정확도만 떨어짐.
- 우리 화면에 표를 하나 더 넣으면 복잡해짐. ("화면에 뭐가 너무 많다" 피드백)
- 조문 원문은 이미 우리 화면에 있으므로, 위임 관계를 볼 때만 넘기면 충분.

**표시 규칙** — 계열마다 버튼 하나
| 상황 | 버튼 |
|---|---|
| 법률+시행령+시행규칙 | 법률 카드에 1개 |
| 시행령·시행규칙만 (법률이 선별에서 빠진 경우) | 시행령 카드에 1개 |
| 서로 다른 계열의 법률 2개 | 각각 1개씩 |
| 행정규칙·조례만 | 없음 (3단비교 대상 아님) |

URL 은 `lsiSeq`(법령일련번호) + `lsId`(법령ID) 조합.
체계도 조회에서 이미 확보하고 있던 값이라 추가 API 호출 불필요.

### 수정 — 되묻기 기본 선택을 "모름" 으로
기존에는 **첫 번째 보기**가 기본 선택이었음.
사용자가 그대로 넘기면 확인되지 않은 조건이 사실처럼 들어가 답이 틀어짐.
→ 보기 중 "모름" 계열을 찾아 기본 선택. 없으면 마지막 보기.
   확실한 것만 골라야 조건이 정확해짐.

---

## v1.8 — 2026-08-09

### 추가 — 하이퍼링크 3종
법제처 원문에는 링크가 세 종류인데 v1.7 은 첫 번째만 처리했음.

| 유형 | 예시 | 연결 대상 |
|---|---|---|
| 「 」 법령 인용 | 「위험물안전관리법 시행령」 | 그 법령 |
| **위임 표기** | **기후에너지환경부령**, 대통령령 | 해당 시행규칙 / 시행령 |
| **조문 상호참조** | 법 제13조제3항, 영 제8조 | 그 법령의 해당 조문 |

- "○○부령·총리령" → 모법의 **시행규칙**, "대통령령" → **시행령** 로 연결.
- "법/영/규칙" 접두사가 붙은 조문 참조를 해당 조문 URL 로 연결.
  접두사 없는 "제7조" 는 같은 법령 안이므로 링크하지 않음.
- 답변과 조문 원문 양쪽에 적용.

### 개선 — 답변 프롬프트 전면 개편
외부 검토(ChatGPT) 제안을 반영해 재작성. 원안은 약 4,000자로 지나치게 길어
매 요청 토큰을 늘리고 공유재산 등 특정 분야에 치우쳐 있었으므로,
**핵심 원칙만 남겨 1,864자로 압축**하고 분야 중립적으로 다듬음.

받아들인 것:
- **"모름" 조건을 임의로 확정하지 마라** — 되묻기에서 "모름" 을 고를 수 있으므로
  실제로 발생하는 상황. 경우를 나눠 설명하도록 지시.
- **호별 분리 설명** — 여러 호를 한 문장으로 뭉뚱그리지 말 것.
- **확정할 수 없는 사항을 밝힐 것** — 정보 부족을 숨기지 않도록.
- 법률→시행령→시행규칙 위계 검토 순서.
- 하위 법령이 없으면 "확인할 수 없다" 고 명시.

덜어낸 것:
- 공유재산 전용 규칙(행정재산/일반재산, 사용허가/대부 등) — 환경 업무에 불필요.
- 중복 조항 다수. 같은 취지가 여러 절에 반복되어 있었음.
- 4단계 답변 형식 강제 — 간단한 질문에도 형식을 채우려 답이 장황해짐.
  "질문이 단순하면 필요한 부분만 간결하게" 를 명시.

기존 강조 표시(**굵게** / __밑줄__ / !!중요!!) 지시는 유지.

### 수정
헤더 버전 불일치 경고 — `main.py` 와 `index.html` 버전 표기를 1.8 로 일치.

---

## v1.7.1 — 2026-08-09

### 수정 — v1.7 의 답변 강조가 실제로 적용되지 않았던 문제
v1.7 에서 답변 프롬프트에 강조 지시(**굵게** / __밑줄__ / !!중요!!)를
넣었다고 했으나 **파일에 반영되지 않았음.**
치환 대상 문구를 "그 밖에 지켜야 할 것:" 으로 잡았는데
실제 파일에는 "그 밖의 규칙:" 이라 매칭이 실패했고, 조용히 넘어감.
화면 렌더링(renderAnswer)만 검증하고 프롬프트는 확인하지 않은 것이 원인.

→ 강조 지시를 실제로 삽입. 【근거】 줄의 수치도 굵게 표시하도록 추가.
→ **v1.7 변경사항 12개 항목을 파일에서 하나씩 대조 검증** 후 산출.

### 수정 — 자치법규 검색어
`target=ordin&query=성남시 화학물질관리법` → **404**.
조례가 "성남시 ○○법" 이라는 이름일 수 없으므로 당연히 없는 검색.
법령명을 그대로 조례 검색에 넘긴 것이 원인.
→ 법령명 대신 **용어**(누출검사, 토양오염 등)로 조례를 찾도록 변경.
   (404 자체는 코드가 잡아서 무시하고 있었으므로 동작 오류는 아니었음)

---

## v1.7 — 2026-08-09

### 수정 — 되묻기가 존재하지 않는 용어를 지어내던 문제 (구조 변경)
"검사 대상 시설이 어떤 종류인가요?" 의 보기가 이렇게 나왔음.
- **특정토양오염유발시설** — 법령에 없는 이름. 정식 명칭은 "특정토양오염관리대상시설"
- **유해화학물질 취급시설** — 화학물질관리법 용어. 누출검사와 무관
- **VOC 배출시설** — 대기환경보전법 용어. 역시 무관

원인은 **실행 순서**였음. 되묻기가 법령 검색보다 먼저 실행되어,
조문을 하나도 보지 못한 상태에서 AI 기억만으로 질문을 만들었음.
이 프로그램의 전제("AI 기억을 믿지 말고 조문을 근거로 한다")가
되묻기 단계에만 적용되지 않았던 셈.

→ **순서 변경**: 용어변환 → 법령검색 → 조문수집 → **되묻기** → 조문선별 → 답변
→ 되묻기 프롬프트에 수집된 **조문 제목 목록을 함께 전달**.
   "목록에 없는 제도·시설·용어를 쓰지 마라", "정식 명칭 그대로 쓰라" 명시.

### 수정 — 목(가·나·다)이 통째로 빠지던 문제
법제처 XML 에서 `<목>` 은 `<호>` 안이 아니라 **`<항>` 바로 아래 형제**로 존재.
호만 훑는 기존 코드로는 목이 수집되지 않았음.
시행령 제8조제2항제4호의 "가. / 나." 가 화면에서 사라진 원인.
→ 항의 자식을 **문서 순서대로** 읽어 호·목을 제자리에 배치. 목은 한 단 들여쓰기.

### 추가 — 되묻기 개선
- **라운드당 최대 9개** 질문 (기존 2개). 라운드 수가 줄어 대기 시간 감소.
- **"추가로 알려줄 내용" 자유 입력칸** — 라운드마다 하나.
  보기에 없는 내용이나 정확한 수치(예: 저장용량 48,000L)를 직접 적을 수 있음.
  개인정보 입력 금지 안내 병기.

### 추가 — 답변 가독성
- **굵게** 기간·주기·수량 등 실무 수치 / __밑줄__ 의무·금지 / 형광 표시 핵심 조건.
  프롬프트에 "남발하지 마라, 온통 굵으면 아무것도 강조되지 않는다" 명시.
- 【근거】【설명】 소제목 스타일 적용.

### 추가 — 법령 하이퍼링크
답변과 조문 원문의 「법령명」 을 **법제처 해당 법령 페이지로 연결**.
법제처 원문에는 있으나 이 화면에는 없던 기능.

### 정리
`main.py` 상단의 중복된 로그 설정 블록 제거 (같은 설정이 두 번 있었음).

---

## v1.6 — 2026-08-09

실사용 검토 3차.

### 수정 — 질문과 무관한 조문이 인용되던 문제
"누출검사 검사주기" 질문에 아래 조문들이 근거로 잡힘.
- 법률 제8조(**타인 토지에의 출입 등**) — 공무원의 토지 출입 허가 규정
- 법률 제12조(**특정토양오염관리대상시설의 신고 등**) — 신고 수리 통지 기한

원인: 조문 선별 단계가 **번호만 보고** 골랐음.
정답은 시행령 제8조(토양오염검사)인데 법률에도 제8조가 있어 함께 선택됨.

→ 선별 프롬프트를 전면 개편.
   - "**제목이 질문과 무관하면 절대 고르지 마라**" 를 최우선 규칙으로 배치
   - 실제 오류 사례를 예시로 명시 ("검사주기" 질문에 "타인 토지에의 출입 등" 금지)
   - 조문 목록 형식을 `[법령명] 제○조(제목)` 으로 바꿔 법령 구분을 명확히
   - 수치를 묻는 질문은 시행령·시행규칙 우선
   - 선택 범위 8~25개 → **5~20개**. "많이 고르기보다 정확히 고르기"

**참고**: 인용 검증(13/13)은 "조문이 실존하는가" 만 확인하며
"질문과 관련 있는가" 는 판단하지 않음. 이 오류는 검증으로 잡히지 않음.

### 수정 — 조문 원문 패널의 빈 카드
법령 카드가 제목·시행일만 있고 **본문이 통째로 비어** 있는 경우가 있었음
(토양환경보전법 시행령, 위험물안전관리법 시행령·시행규칙).
조문을 하나도 못 가져왔을 때 아무것도 렌더링하지 않아 오류처럼 보임.
→ "조문 본문을 가져오지 못했습니다" 안내를 표시.

### 개선 — 근거 조문 텍스트 복사
카드 전체가 클릭 영역이라 **본문을 드래그하면 접히거나 펼쳐져** 복사가 불가능했음.
→ **헤더만 클릭 영역**으로 분리. 본문은 자유롭게 선택·복사 가능.
→ 헤더 우측에 ▸/▾ 표시 추가로 접힘 상태를 명확히.

---

## v1.5 — 2026-08-08

외부 검토(Gemini) 지적 사항 반영.

### 수정 — 법률/시행령 조문을 뒤섞어 인용하던 문제 (심각)
법률 제8조("타인 토지에의 출입 등")를 **시행령 제8조**라고 인용하고,
거기에 누출검사 관련 내용을 갖다 붙이는 오류가 확인됨.

원인: 컨텍스트가 `=== 법령명 ===` 구분선으로만 나뉘어 있어,
조문이 길어지면 AI 가 아래로 갈수록 어느 법령 소속인지 잊음.

→ **조문마다 앞에 법령명을 직접 표기**. `[토양환경보전법] 제8조(타인 토지에의 출입 등)`
→ 구분선 문구도 명시적으로 변경. "여기부터는 「○○」 조문입니다"
→ 답변 프롬프트에 법령명 표기 규칙을 최상단 배치:
   번호만 보고 짐작 금지, 조문 제목과 내용이 어울리는지 재확인,
   위임 관계를 지어내지 말 것.
→ **단계 불일치 자동 경고** — AI 가 "시행령" 이라고 적었는데 실제로는
   법률 조문이면 근거 조문 카드에 붉은 경고를 표시.

### 수정 — 키 쿨다운 기본값 300초 → 15초
분당 한도(TPM/RPM)는 1분이면 회복되므로 5분은 과했음.
응답의 `retryDelay` 가 오면 그 값을 우선하고,
일일 한도(RPD)로 판별되면 1시간으로 자동 연장.

### 확인 — 부처 명칭은 오류 아님
"기후에너지환경부장관", "기후에너지환경부령" 표기는 정상.
2025.10.1. 정부조직법 개정으로 환경부 → 기후에너지환경부로 변경됨.
법제처 원문에 `<개정 2025.10.1>` 이 달려 있고 개정이유에도 명시되어 있음.
**API 가 최신 데이터를 정확히 주고 있는 것**이며, 검토한 AI 의 지식이
개정 이전에 머물러 생긴 오해.

---

## v1.4 — 2026-08-08

실사용 검토 결과 반영. (위험물안전관리법 오검색 건)

### 수정 — 엉뚱한 법령을 찾던 문제 (가장 중요)
"누출검사 검사주기" 질문에 **위험물안전관리법**(옥외탱크 구조안전점검)이 잡혀
토양환경보전법의 누출검사와 전혀 다른 답이 나왔음. 용어는 같지만 다른 제도.

원인은 두 가지.
- v1.2 에서 법령명 후보를 2개 → 1개로 줄여, 빗나가면 통째로 틀리는 구조가 됨
- 용어 변환 프롬프트에 **분야 맥락이 없어** 어느 법을 우선할지 판단 근거가 없었음

→ 프롬프트에 "질문자는 지방자치단체 환경 담당 공무원" 을 명시하고,
   같은 용어가 여러 법에 쓰일 때 **환경 분야 법령을 우선**하도록 지시.
   소방·위험물이 질문에 명시된 경우에만 그쪽을 1순위로.
→ 법령명 후보를 다시 2개로 복원. v1.3 의 조문 선별로 토큰 부담이 사라졌으므로
   범위를 넓혀도 됨. 수집 법령도 5개 → 7개.

### 수정 — 근거 조문 중복
같은 조문이 두 번씩 표시됐음. 답변의 【근거】 블록과 【설명】 양쪽에서
인용되는데, 중복 제거 키가 (법령명, 라벨) 이라 표기가 조금만 달라도 걸러지지 않음.
→ (법령명, 조, 가지, 항, 호) 정규화 키로 변경. 실측 6건 → 3건.

### 수정 — "제목 없음"
`조문제목` 태그가 비어 있는 법령이 있음 (위험물안전관리법 시행규칙 제64조 등).
오른쪽 원문에는 "제64조(정기점검의 횟수)" 로 나오는데 근거 조문에는 "제목 없음".
→ 태그가 비면 **본문 첫머리의 괄호 제목을 추출**하도록 보완. 자치법규도 동일 적용.

### 메모 — 모델명
`gemini-2.5-flash-lite` 는 신규 키에서 404(no longer available to new users).
`.env.example` 기본값을 `gemini-3.5-flash-lite` 로 변경.
대체 모델도 `gemini-3.5-flash,gemini-2.5-flash` 로 조정.

---

## v1.3 — 2026-08-08

### 추가 — 조문 선별 (토큰 약 70% 절감)
조문 본문을 통째로 넣어 요청당 약 3만 토큰을 쓰고 있었음.
무료 등급은 분당 토큰(TPM)이 병목이라 검색 두세 번이면 한도에 닿음.

→ **2단계 구조로 변경**
```
1) 조문 제목만 나열해 AI 에게 보여줌  → "어떤 조문이 필요한가"   (약 2,700자)
2) 고른 조문의 본문만 넣어 답변 생성                          (약 18,000자)
```
AI 호출이 1회 늘지만 그 1회가 가벼워 전체적으로 크게 줄어듦.
실측 기준 69,300자 → 20,695자 (**71% 감소**).

**고르는 주체는 AI 이며 사용자는 아무것도 하지 않습니다.**
법을 잘 모르는 사람에게 조문 선택을 시키면 잘못 골랐을 때
틀린 답이 그럴듯하게 나오므로, 그 방식은 채택하지 않음.

**누락 방지 장치**
- 프롬프트에 "넉넉히 골라라, 빠뜨리는 것이 잘못 넣는 것보다 나쁘다" 명시
- 절차 질문이면 앞뒤 단계 조문, 기준·기한 질문이면 시행령·시행규칙 포함 지시
- 선별 실패 시 자동으로 전체 조문 사용
- 조문이 12개 이하면 선별하지 않고 전체 사용
- **오른쪽 원문 패널에는 전체 조문이 그대로 남음** — AI 가 놓쳐도 사용자가 확인 가능
- `.env` 의 `SELECT_ARTICLES=0` 으로 언제든 되돌릴 수 있음

처리 과정에 "조문 선별 — 77개 중 20개 선택" 표시.

### 참고 — 되묻기는 토큰을 거의 쓰지 않음
되묻기 단계는 조문을 넣지 않아 회당 500~600토큰 수준.
토큰의 99%가 답변 생성 한 번에 몰려 있으므로,
되묻기 횟수는 토큰 부담 없이 늘려도 됨.

---

## v1.2 — 2026-08-08

### 추가 — 단계별 모델 분리
단계마다 필요한 능력이 다른데 전부 같은 모델을 쓰고 있었음.

| 단계 | 난이도 | `.env` 항목 |
|---|---|---|
| 용어 변환 | 낮음 | `GEMINI_MODEL_TERMS` |
| 되묻기 | **높음** | `GEMINI_MODEL_CLARIFY` |
| 답변 생성 | 중간 (토큰 대부분) | `GEMINI_MODEL_ANSWER` |

되묻기는 출력이 짧고 조문을 넣지 않아 비싼 모델을 써도 비용 영향이 작음.
비어 있으면 `GEMINI_MODEL` 을 사용.

### 추가 — 인증키 자동 전환
`GEMINI_API_KEY=키1,키2,키3` 처럼 쉼표로 나열.
429(한도 초과)가 나면 그 키를 쿨다운 목록에 넣고 **자동으로 다음 키로 전환**.
쿨다운(`GEMINI_KEY_COOLDOWN`, 기본 300초)이 지나면 자동 복귀.
서버 재시작 불필요. 헤더에 `키 2/3` 형태로 상태 표시.

**주의**: 무료 한도는 API 키가 아니라 **프로젝트 단위**로 적용됨.
같은 프로젝트에서 키만 늘리면 한도는 그대로임. `.env.example` 에 명시.

### 추가 — 조문 컨텍스트 상한
`CONTEXT_LIMIT` (기본 60,000자). 무료 등급은 **분당 토큰(TPM)이 병목**이라
요청당 토큰을 줄이는 것이 한도 초과를 막는 가장 확실한 방법.
측정 결과 요청당 약 3만 토큰을 쓰고 있었음 — TPM 25만 기준 분당 8건이면 소진.
처리 과정에 "→ 60000자로 축소" 표시.

---

## v1.1 — 2026-08-08

### 수정 — 근거 조문이 모두 같은 내용으로 보이던 문제
"제8조제1항제2호", "제8조제2항제4호", "제8조제2항제5호", "제8조제3항" 이
전부 **제8조 전체**를 그대로 보여주고 있었음. 조문 단위로만 저장하고
항·호 단위로 쪼개지 않았기 때문.

→ `extract_hang()` 추가. 인용한 **항(①②③)과 호(1. 2. 3.)만 잘라서** 표시.
→ 조문 전체가 필요하면 카드 안의 "조문 전체 보기" 로 펼침.

### 수정 — 항 줄바꿈이 안 되던 경우
법제처 원문은 `…아니한다.<개정 2017.11.28>② 제1항…` 처럼
**항 사이에 공백이 없는** 경우가 많아 정규식이 어긋났음.
또 본문을 길이로 자를 때 `<개정 2017.` 같은 조각이 끝에 남았음.

→ 공백 없이 붙은 항·호도 잡도록 수정. 잘린 끝 조각 제거.
→ **`[가-하]` 버그 수정** — 이 범위는 한글 음절 전체를 포함하므로
   "받을 것." 같은 평범한 문장을 "목"으로 오인해 엉뚱하게 줄이 갈렸음.
   실제 목 글자(가나다라마바사아자차카타파하)만 나열하도록 변경.

### 제거
- **조문 안에서 검색** — v1.0에서 인용된 조문만 표시하도록 바꾸면서
  원문이 짧아져 효용이 사라짐. 화면만 복잡해져 제거.

---

## v1.0.1 — 2026-08-08

### 수정 — 인용 조문이 전부 "법률"로 표시되던 버그
인용 검증이 **조문 번호만으로** 매칭하고 있었음.
법률과 시행령은 조문 번호가 겹치므로(둘 다 제3조가 존재),
AI가 "시행령 제3조제1항제4호"라고 써도 법률 제3조로 붙어
계층 배지가 전부 "법률"로 나오고 조문 제목도 엉뚱하게 표시됨.

→ 인용문 앞의 **법령명까지 함께 매칭**하도록 수정.
   - 실제 수집한 법령 이름과 뒤에서부터 대조 (긴 이름 우선)
   - "시행령 제3조" 처럼 단계만 쓴 약식 표기도 처리
   - 법령명 추정이 빗나가면 번호만으로 재탐색하는 폴백 유지
→ 조문 하이라이트 키도 `법령명|조|가지` 로 변경.

---

## v1.0 — 2026-08-08

가독성 개선. 실사용 피드백 반영.

### 수정 — 조문 본문 줄바꿈
법제처 원문은 항(①②③)·호(1. 2. 3.)가 **한 줄로 붙어** 있어 그대로 두면 읽을 수 없었음.
`formatBody()` 로 항·호·목 단위 줄바꿈, 내어쓰기 적용.
`<개정 2017.11.28>` 같은 연혁 표기는 흐리게 처리.
조문 제목이 본문 앞에 중복되던 것도 제거.

### 수정 — 근거 조문 영역
- **계층 소제목** — 법률 / 시행령 / 시행규칙 / 확인 안 됨 순으로 묶어 표시.
  어느 단계 조문인지 한눈에 구분됨.
- 각 카드에 계층 배지 추가. 글자 잘림(`overflow`) 수정, 줄간격·줄바꿈 보정.
- 펼친 조문 본문도 항 단위로 줄바꿈. 표시 길이 600 → 900자.

### 수정 — 무관 법령 정리
- 답변에 인용되지 않은 법령은 **흐리게 + 접힌 상태**로 표시하고
  "답변에 인용되지 않았습니다" 라고 명시.
  (위임행정규칙은 자동으로 딸려오므로 인용 0건인 카드가 생김)
- **검색 범위 축소** — 법령명 후보 2개 → 1개, 위임행정규칙 6개 → 4개,
  본문 수집 6개 → 5개.
  "누출검사" 가 화학물질관리법 계열까지 끌어와 19개 법령·21만자를 수집하던 문제.
  조문이 많을수록 답변 정확도도 떨어짐.

---

## v0.9.1 — 2026-08-08

- **되묻기 라운드를 `.env` 로 조절** — `CLARIFY_ROUNDS` (기본 6).
  라운드마다 Gemini 호출이 1회 늘어나므로, 무료 한도가 넉넉해지면 올리면 됨.
  더 물을 것이 없으면 이 횟수와 관계없이 자동 종료.
- 되묻기 프롬프트에 **좁혀가는 순서** 명시
  (시설 종류 → 설치 시기 → 구조·방지시설 → 위반·처분 단계 → 관할).
  단, 억지로 질문을 만들지 말고 남은 것이 없으면 끝내라고 못박음.
- `.env.example` 에 `CLARIFY_ROUNDS`, `GEMINI_FALLBACK` 항목 추가.

---

## v0.9 — 2026-08-08

Gemini 과부하 대응, 다회 되묻기, 로그 시각.

### 수정 — 오류가 사용자에게 전달되지 않던 문제
앱 오류를 `status_code=502` 로 내보내고 있었는데,
**Cloudflare 가 5xx 응답 본문을 자체 HTML 오류 페이지로 교체**해버려
실제 오류 메시지가 화면에 도달하지 못했음. (`Unexpected token '<'`)

→ 앱 오류·한도 초과를 모두 **200 + error/quota 필드**로 변경.
   화면도 상태 코드가 아니라 필드를 보고 판단하도록 수정.

### 추가
- **503 재시도 + 대체 모델** — 503(UNAVAILABLE)은 모델 수요 폭증에 따른
  구글 쪽 일시 장애로, 키를 바꿔도 소용없음.
  같은 모델로 2초→4초→8초 간격 재시도 후, 안 되면 대체 모델로 전환.
  `.env` 의 `GEMINI_FALLBACK=모델1,모델2` 로 지정.
- **다회 되묻기 (최대 3라운드)** — 기존에는 1회로 끝났음.
  이전 라운드 답변을 누적해 넘기고, 같은 것을 다시 묻지 않도록 프롬프트에 명시.
  화면에 "1 / 3" 진행 표시와 "지금까지: …" 누적 조건 표시.
- **서버 로그에 날짜·시각** — uvicorn 기본 로그에 시각이 없어 추가.
  `2026-08-08 05:12:33 INFO ...` 형태.
- 오류 시 근거 조문·조문 원문 패널의 스피너가 계속 돌던 문제 수정.
- 비JSON 응답(로그인 페이지·프록시 오류)을 구분해 안내.
- **버전 표시** — 헤더에 `v0.9`. 서버와 화면 버전이 다르면 빨간 경고.
  (v0.8 적용 시 index.html 만 교체하고 파이썬 파일을 안 바꿔 발생한 사고 재발 방지)

### 메모
되묻기는 Gemini 호출을 라운드마다 1회씩 추가로 씁니다.
질문자가 법령에 익숙하지 않은 것이 전제이므로, 토큰보다 질문 품질을 우선함.

---

## v0.8 — 2026-08-08

법령체계도 도입. 검색 구조 전면 개편.

### 핵심 — 법령체계도(`target=lsStmd`)
응답의 `<상하위법>` 안에 법률 → 시행령 → 시행규칙이 중첩돼 있고,
**각 단계마다 `<행정규칙>` 블록이 딸려 옴.**

```
<법률>
  <시행령>
    <시행규칙>
      <행정규칙><고시>토양정밀조사의 세부방법에 관한 규정</고시></행정규칙>
    </시행규칙>
    <행정규칙><예규>특정토양오염관리대상시설 관리지침</예규></행정규칙>
  </시행령>
  <행정규칙><예규>토양오염실태조사지침</예규></행정규칙>
</법률>
```

→ 법령 하나만 잡으면 **위임 고시·지침까지 통째로** 딸려옴. 이름으로 검색할 필요 없음.
→ 위임행정규칙용 별도 API 불필요. 체계도 하나로 해결.

### 추가
- **B-1 통합 검색** — 드롭다운 기본값이 "전체". 체계도로 법령+행정규칙을 한 번에 수집.
- **B-2 위임행정규칙 연결** — 어느 단계(법률/시행령/시행규칙)에 위임됐는지 배지로 표시.
- **A-1 인용 조문만 표시** — AI가 인용한 조문만 펼치고, 나머지는 "전체 조문 보기"로 접음.
  기존에는 18만자를 그대로 쏟아내 사실상 읽을 수 없었음.
- **A-3 계열별 그룹핑** — 법령 / 위임 행정규칙 / 자치법규를 구분해 표시.
- **C-1~C-3 되묻기** — 적용 조문이 갈릴 만하면 라디오 선택지를 제시.
  명확한 질문은 그냥 통과. "건너뛰고 그냥 검색" 우회 버튼 제공.
- **D-2** 햄스터 씨앗 좌표 버그 수정 (`getBoundingClientRect`).

### 확인된 것 (개발 보류)
- **B-4 연혁**(`target=lsHistory`) — 동작하나 `type=XML` 이 안 먹고 HTML 반환.
  파싱 가능하지만 우선순위 낮아 보류.

### 미착수
- A-2 3단비교 (법률/시행령/시행규칙 가로 배치)
- D-1 신구법비교
- E-1 첫 화면 가독성 (실사용 피드백 후)

---

## v0.7 — 2026-08-07

자치법규 본문 파싱 완성. **조용한 오조회 버그 수정.**

### 치명적 버그 — MST 파라미터가 받는 값
`MST=2111431`(성남시 도시계획 조례의 **일련번호**)로 조회했더니
**인천광역시 조례**가 반환됨. 에러가 아니라 정상 응답으로.

- `MST` 는 **`자치법규ID`** 를 받음 (`자치법규일련번호` 아님)
- 잘못된 값을 넣으면 오류 없이 **다른 지자체 조례**가 조용히 나옴

→ `ID_KEYS["ordin"]` 순서를 `자치법규ID` 우선으로 수정.
→ **이중 안전장치**: 목록의 법규명과 본문의 `자치법규명` 이 다르면 결과를 버림.

### 추가 — 자치법규 조문 구조
법령·행정규칙과 또 다른 세 번째 구조.

| | 태그 |
|---|---|
| 최상위 | `<LawService>` |
| 기본정보 | `<자치법규기본정보>` |
| 조문 | `<조문>/<조>` — `<조제목>`, `<조내용>` (법령의 `조문제목`/`조문내용` 과 다름) |
| 조문번호 | `000702` 형태 **6자리 0패딩**. 앞 4자리=조, 뒤 2자리=가지번호 → "제7조의2" |

- 별표 본문이 비고 첨부파일(hwp)만 있는 경우 안내 문구로 대체하고 내려받기 링크 연결.

### 확인된 것
- 담당부서명·전화번호가 기본정보에 포함됨 (예: 생태하천과 031-729-3181) — 실무에 유용
- 타법 인용 추출이 조례에서도 동작 (「물환경보전법」, 「성남시 물관리 기본 조례」)

---

## v0.6 — 2026-08-07

자치법규(성남시 조례) 연결.

### 배경
`/api/raw?target=ordin&...&mode=search` 응답 확인 결과, 두 가지 함정이 있었음.

- 레코드 태그가 `<ordin>` 이 아니라 **`<law>`**
- 본문 조회 파라미터가 `ID=` 가 아니라 **`MST=`**
  (응답의 `자치법규상세링크` 가 `...&target=ordin&MST=2092025` 형태)
  → `ID=` 로 호출하면 본문이 나오지 않음

### 수정
- `DETAIL_PARAM` 매핑 추가. target 별로 `ID` / `MST` 를 구분해 사용.
- 자치법규 ID 는 `자치법규일련번호` 사용 (`자치법규ID` 아님).
- `dump_raw` 에도 동일 적용.

### 추가
- 자치법규 검색 시 **"성남시" 자동 접두** — 전국 조례가 섞이는 것을 방지.
  검색어에 이미 "성남"이 있으면 그대로 사용.
- 화면 소관 표시에 `지자체기관명` 우선순위 반영.

### 미확인
- ~~자치법규 본문 구조~~ → v0.7 에서 해결

---

## v0.5 — 2026-08-07

행정규칙 본문 파싱 완성. **별표 텍스트 확보.**

### 배경
`/api/raw?target=admrul&...&mode=detail` 로 실제 응답 구조를 확인한 결과,
행정규칙은 법령과 구조가 전혀 다름.

| | 법령 | 행정규칙 |
|---|---|---|
| 최상위 | `<법령>` | `<AdmRulService>` |
| 기본정보 | `<기본정보>` | `<행정규칙기본정보>` |
| 조문 | `<조문>/<조문단위>` (번호·제목 태그 있음) | `<조문내용>` 이 루트 바로 아래 **평평하게 나열, 태그 없음** |
| 현행 표시 | `현행연혁구분` | `현행여부` (Y/N) |

### 추가
- **행정규칙 평문 조문 파싱** — 본문 텍스트에서 정규식으로 `제3조의2(제목)` 추출.
  가지번호(제4조의2)와 장·절 제목("제1장 총칙") 구분 처리.
- **별표·별지·서식 추출** — 행정규칙 응답에 `<별표단위>` 로 **본문 텍스트가 함께 옴**.
  제목·내용을 조문 목록에 포함하고, 화면에서 금색 배경으로 구분 표시.
- **별표 원본 내려받기 링크** — `별표서식PDF파일링크` 를 화면에 연결.
- AI 컨텍스트에 별표 포함 (표 형태라 2,500자로 제한).

### 참고
별표가 텍스트로 오는 것은 예상 밖의 수확.
법령 시행규칙의 별표(우려기준·검사주기 등)도 같은 방식으로 잡힐 가능성이 있음 — 확인 필요.

---

## v0.4 — 2026-08-07

계층 확장과 안정성 보강.

### 추가
- **계층 통합 검색** — 법률 → 시행령 → **시행규칙**까지 자동 수집 (최대 4개 법령).
  기존에는 법률·시행령만 가져와 우려기준·검사주기 등 별표 수치가 누락됐음.
- **행정규칙·자치법규·법령해석례 대응** — `target=admrul / ordin / expc` 파싱 추가.
- **폐지·연혁본 자동 제외** — 목록의 `현행연혁구분` 필드로 필터링.
- **타법 인용 자동 추출** — 조문 속 「법령명」을 뽑아 버튼으로 표시. 환경↔토목 연결 지점 탐지.
- **한도 초과 팝업** — Gemini 429 응답에서 분당/일일 한도를 구분해 안내. 분당이면 초 카운트다운.
- **조회 이력 브라우저 저장** — localStorage. 새로고침해도 유지, PC별로 분리, 서버에 미저장.
- **조문 내 검색** — 하이라이트 + 자동 스크롤.
- **답변 복사 버튼**, **예시 질문 4종**, **이력 비우기**.
- **최근 개정 배지** — 시행일이 6개월 이내면 "최근 개정", 미래면 "시행 예정" 표시.

### 수정
- 행정규칙 본문 조회 ID를 `행정규칙ID`(37926)가 아닌
  **`행정규칙일련번호`(2100000267852)** 로 변경. 전자로는 본문이 조회되지 않음.

### 미확인 — 다음 작업 대상
- ~~행정규칙 본문 XML 구조~~ → v0.5 에서 해결
- ~~자치법규 목록 구조~~ → v0.6 에서 해결 (본문은 미확인)
- ~~별표·서식 텍스트 추출~~ → v0.5 에서 행정규칙 별표 확보

---

## v0.3 — 2026-08-07

인용 정확도 대응.

### 추가
- **근거 조문 영역** — 답변 상단에 `법령명 + 제○조제○항` 목록. 클릭 시 원문 펼침.
- **서버측 인용 검증** — AI가 인용한 조문 번호를 실제 조문 목록과 대조.
  존재하지 않으면 붉게 표시. 처리 과정에 "인용 검증 n/m건" 표시.
- 프롬프트에 【근거】/【설명】 블록 강제. 호 번호를 뭉뚱그리지 말 것을 명시.

### 배경
v0.2 테스트에서 Gemini가 서로 다른 호에 해당하는 세 항목을
모두 `제14조제1항제2호`로 표기하는 오류 발생. 조 단위 검증으로 대응.

### 한계
검증은 **조 단위**까지. 항·호 정확성은 자동 검증 불가 — 원문 대조 필요.

---

## v0.2 — 2026-08-07

조문 파싱 정상화 및 UX.

### 수정
- **조문 파싱 전면 수정** — `<조문>/<조문단위>` 만 읽도록 변경.
  기존에는 문서 전체를 훑어 **부칙·별표(과태료표)** 가 조문으로 잡혔음.
- 법령명 태그를 `법령명한글` → **`법령명_한글`**(밑줄) 로 정정.
- `<항>` 내용을 조문 본문에 이어붙이도록 수정. 기존에는 조문 제목만 수집됨.
- 장·절 제목(`조문여부=전문`)을 AI 컨텍스트에서 제외.

### 추가
- 조회 중 스피너 + 진행 문구.
- 하단 출처 표기 (법제처 API / Gemini / FastAPI / 참고 오픈소스).

### 프롬프트
과도하게 방어적인 "확인되지 않음" 응답 방지.
질문 표현과 조문 표현이 달라도 대응하면 관련 조문으로 인정하도록 완화
(예: "주유소" = "특정토양오염관리대상시설").

---

## v0.1 — 2026-08-07

최초 프로토타입.

### 구성
- `law_client.py` — 법제처 OPEN API 호출 및 XML 파싱
- `ai_client.py` — Gemini 호출 (모델 교체 시 이 파일만 수정)
- `main.py` — FastAPI 서버, 라우팅, 로그인
- `static/index.html` — 화면 전체 (HTML+CSS+JS 단일 파일)

### 기능
- 질문 → 법령용어 변환 → 법령 검색 → 조문 조회 → 답변 생성
- 조문 원문 병기 + **시행일·공포번호 배지** (구버전 인용 방지)
- 비밀번호 로그인 (쿠키 HMAC 서명, `.env`의 `APP_PASSWORD`)
- 원본 XML 확인용 `/api/raw`

### 환경
- Python 3.13 / FastAPI / uvicorn
- 법제처 OPEN API (open.law.go.kr, 자동승인)
- Gemini `gemini-3.6-flash`
- 외부 공개: Cloudflare Tunnel

---

## 알려진 제약

| 항목 | 내용 |
|---|---|
| 시청 업무망 | `trycloudflare.com` 차단됨. 휴대폰 데이터로는 접속 가능. 도메인 구매 검토 중 |
| 터널 주소 | 재시작 시 변경됨. named tunnel 또는 도메인 필요 |
| AI 정확도 | 조·항·호 오기 가능. **오른쪽 조문 원문 대조 필수** |
| 개인정보 | 질문이 외부 AI로 전송됨. 민원인 정보 입력 금지 안내 표시 중 |
| 법적 효력 | 참고용. 효력 기준은 관보 |

---

## 다음 작업 후보

1. 법령 시행규칙 별표(우려기준·검사주기) 텍스트 확보 여부 확인
2. 법령 시행규칙 별표(우려기준·검사주기) 확보 여부 확인
3. 라즈베리파이 이전 + systemd 자동 시작
4. 고정 도메인 연결 (Cloudflare named tunnel)
5. 개인정보 패턴 자동 필터 (전송 전 차단)
6. 별표·서식 첨부파일 처리
